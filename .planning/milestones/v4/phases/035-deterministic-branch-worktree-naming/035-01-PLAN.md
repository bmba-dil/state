---
phase: 035-deterministic-branch-worktree-naming
plan: 01
type: execute
wave: 0
depends_on:
  - 032
files_modified:
  - src/state_core/worktree_naming.py
  - tests/test_worktree_naming.py
autonomous: true
requirements:
  - WRK-05
must_haves:
  truths:
    - "format_branch produces deterministic slice/<arc>/<phase>/<slice> names"
    - "IDs are sanitized (special chars → dashes, lowercase)"
    - "find_collision queries WorktreeService.list()"
---

<objective>
Implement deterministic worktree branch naming in `src/state_core/worktree_naming.py`. Produces `slice/<arc>/<phase>/<slice-id>` branch names per WRK-05 with ID sanitization and collision detection.
</objective>

<read_first>
Read before implementing:
- src/state_core/worktree.py (WorktreeService Protocol)
</read_first>

### Tasks

#### 035.1 Implement naming module

<action>
Create `src/state_core/worktree_naming.py`:

1. `_sanitize_id(raw: str) -> str` — lowercase, replace `[^a-z0-9-]` with `-`, trim leading/trailing dashes
2. `format_branch(arc_id: str, phase_id: str, slice_id: str) -> str` → `"slice/{arc}/{phase}/{slice}"`
3. `format_worktree_name(arc_id: str, phase_id: str, slice_id: str) -> str` — same as format_branch (names = branches)
4. `async def find_collision(service: WorktreeService, arc_id: str, phase_id: str, slice_id: str) -> bool` — checks if worktree with this name already exists
</action>

<acceptance_criteria>
- `from state_core.worktree_naming import format_branch` works
- Same inputs → same output (deterministic)
- Special chars sanitized
</acceptance_criteria>

<estimated_effort>Small</estimated_effort>
<dependencies>032</dependencies>

#### 035.2 Write tests

<action>
Create `tests/test_worktree_naming.py`:

1. Test format_branch produces expected output
2. Test determinism (same inputs → same output)
3. Test sanitization (spaces, uppercase, special chars)
4. Test empty IDs
5. Test find_collision with mock WorktreeService
</action>

<acceptance_criteria>
- `python3 -m pytest tests/test_worktree_naming.py -v` passes
</acceptance_criteria>

<estimated_effort>Small</estimated_effort>
<dependencies>035.1</dependencies>

<verification>
Run: `python3 -m pytest tests/test_worktree_naming.py -v`
</verification>

<must_haves>
- format_branch deterministic
- ID sanitization
- Collision detection
- All tests pass
</must_haves>
