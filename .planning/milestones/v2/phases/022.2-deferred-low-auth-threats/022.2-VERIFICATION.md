---
phase: 022.2-deferred-low-auth-threats
verified: 2026-05-02T00:00:00Z
status: passed
score: 15/15 must-haves verified
re_verification:
  is_re_verification: false
verification_mode: initial
quality_level: fast
plans_verified:
  - 022.2-01-PLAN.md (T-018-9 symlink defense)
  - 022.2-02-PLAN.md (T-018-8 vault-file race)
requirements_covered: [AUTH-06]
test_baseline:
  full_suite: 772 passed / 0 failed
  auth_suite: 412 passed / 0 failed
code_review_status: issues_found (advisory) — 0 MUST/SHOULD FIX, 4 INFO
---

# Phase 022.2: Deferred LOW Auth Threats — Verification Report

**Phase Goal:** Close two LOW-severity threats deferred from Phase 018:
- **T-018-9** — Symlink-attack mitigation on `auth.json` (refuse to follow symlinks when opening the vault).
- **T-018-8** — Vault-file race during concurrent CLI reads/writes outside the existing filelock window.

**Verified:** 2026-05-02
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths — Plan 01 (T-018-9 symlink defense)

| #   | Truth | Status | Evidence |
| --- | ----- | ------ | -------- |
| 1   | `_atomic_write` opens tmp file with `O_NOFOLLOW`; symlinked tmp paths rejected with `AuthVaultSymlinkError` | VERIFIED | `store.py:245` — `os.O_WRONLY \| os.O_CREAT \| os.O_TRUNC \| os.O_NOFOLLOW`; `:250` re-raises ELOOP as `AuthVaultSymlinkError(tmp) from exc` |
| 2   | `_verify_mode` uses `os.lstat` (never `os.stat`); symlink at vault path rejected before any read | VERIFIED | `store.py:200,205` — `os.lstat(path)` on both POSIX and Windows branches; `:202,207` raise `AuthVaultSymlinkError(path)` if `S_ISLNK` |
| 3   | `ensure_initialized` lstat-checks existing path before delegating to `_verify_mode` | VERIFIED | `store.py:335-336` — `if path.is_symlink(): raise AuthVaultSymlinkError(path)` |
| 4   | `AuthVaultSymlinkError` is OSError subclass exported from `state_core.auth.store`; message contains offending path but NOT link target contents | VERIFIED | `store.py:87` — `class AuthVaultSymlinkError(OSError)`; `:346` exported via `__all__`; constructor stores only `self.path` and a static error string (no `os.readlink` call) |
| 5   | Previously-skipped `test_symlink_attack_rejected` is unmarked and passes | VERIFIED | `grep -c "@pytest.mark.skip" tests/auth/test_store.py` → 0; STORE-22a..f all replace it |
| 6   | At least 4 distinct symlink-attack scenarios exercised and ALL pass | VERIFIED | 6 distinct test functions present (STORE-22a..f): `test_symlink_at_vault_path_rejected`, `test_symlink_at_tmp_path_rejected`, `test_symlink_parent_dir_rejected`, `test_ensure_initialized_rejects_symlink`, `test_symlink_error_message_hygiene`, `test_regular_file_still_loads` |
| 7   | `store.py` module docstring no longer claims symlink defense is OUT OF SCOPE | VERIFIED | `grep -c "OUT OF SCOPE" store.py` → 0; `:25` reads "CLOSED by Phase 022.2" |
| 8   | 765-test baseline regresses to 0 failures and 0 skipped symlink placeholder tests; passing tests rise by ≥4 | VERIFIED | `tests/auth/test_store.py` 21→27 passed (+6), 1→0 skipped; full auth suite 412 passed / 0 failed |

### Observable Truths — Plan 02 (T-018-8 vault-file race)

| #   | Truth | Status | Evidence |
| --- | ----- | ------ | -------- |
| 9   | `state_core.auth.refresh` exposes a SYNC filelock context manager (`with_vault_lock`) suitable for CLI use | VERIFIED | `refresh.py:178` — `def with_vault_lock(...)` decorated with `@contextlib.contextmanager`; `:427` exported via `__all__` |
| 10  | `cli_ops.login()` holds vault lock across `load_vault → mutate → save_vault` window | VERIFIED | `cli_ops.py:276` — `with with_vault_lock(vault_path):` wraps the persist block |
| 11  | `cli_ops.logout()` holds vault lock across `load_vault → mutate → save_vault` window | VERIFIED | `cli_ops.py:325` — `with with_vault_lock(vault_path):` wraps the RMW region |
| 12  | Concurrent CLI-style writers (≥4 threads) do NOT clobber each other; final vault.providers length equals N | VERIFIED | `tests/auth/test_cli_ops.py:473` — `test_concurrent_login_no_clobber` (N=4 ThreadPoolExecutor, asserts `len(creds) == 4`); `:516` — `test_concurrent_logout_no_clobber` (asserts `len(remaining) == 0`) |
| 13  | Lock primitive used by cli_ops is the SAME logical lock as Phase 013's refresh lock (same `<vault>.lock` sibling path) | VERIFIED | `refresh.py:212-216` — `filelock.FileLock(str(_lock_path_for(vault_path)), ...)` uses the same `_lock_path_for` helper that `_new_async_lock` uses |
| 14  | `RefreshLockTimeout` propagates from cli_ops if lock cannot be acquired within 10s | VERIFIED | `refresh.py:228` — `except filelock.Timeout as exc: raise RefreshLockTimeout(lock_path) from exc` inside `with_vault_lock` |
| 15  | Existing baseline regresses to 0 failures; passing tests rise by ≥1 | VERIFIED | `tests/auth/test_cli_ops.py` 14→16 passed (+2 concurrent tests); full suite 772 passed / 0 failed |

**Score:** 15/15 truths verified

### Required Artifacts

| Artifact | Expected | Exists | Substantive | Wired | Status |
| -------- | -------- | ------ | ----------- | ----- | ------ |
| `src/state_core/auth/store.py` | `AuthVaultSymlinkError` + O_NOFOLLOW + lstat | Yes | Yes (class at :87, O_NOFOLLOW at :245, lstat at :200/205, parent lstat at :237) | Yes (imported by `__init__.py:52` and `tests/auth/test_store.py:50`) | VERIFIED |
| `src/state_core/auth/__init__.py` | Re-exports `AuthVaultSymlinkError` | Yes | Yes (line :52, :78) | Yes | VERIFIED |
| `src/state_core/auth/refresh.py` | `_new_sync_lock` + `with_vault_lock` context manager | Yes | Yes (`_new_sync_lock` at :157; `with_vault_lock` at :178; `__all__` at :427) | Yes (imported by `cli_ops.py:28`) | VERIFIED |
| `src/state_core/auth/cli_ops.py` | login/logout wrap RMW in `with_vault_lock` | Yes | Yes (2 `with with_vault_lock` blocks at :276, :325; import at :28) | Yes | VERIFIED |
| `tests/auth/test_store.py` | STORE-22 symlink test suite (≥4, no skip) | Yes | Yes (6 `def test_symlink_*` / `test_ensure_initialized_rejects_symlink` / `test_regular_file_still_loads` functions; 0 `@pytest.mark.skip` markers) | N/A (test file) | VERIFIED |
| `tests/auth/test_cli_ops.py` | T-018-8 concurrent-writer regression test | Yes | Yes (2 `def test_concurrent_*` functions using ThreadPoolExecutor) | N/A (test file) | VERIFIED |

### Key Link Verification

| From | To | Via | Status | Detail |
| ---- | -- | --- | ------ | ------ |
| `store.py::_atomic_write` | kernel ELOOP semantics | `os.open(tmp, O_WRONLY \| O_CREAT \| O_TRUNC \| O_NOFOLLOW, 0o600)` | WIRED | `store.py:245`; ELOOP catch at `:248-250` translates to domain exception |
| `store.py::_verify_mode` | kernel symlink-vs-regular-file check | `os.lstat(path) + stat.S_ISLNK(mode)` | WIRED | `store.py:200,205` — both POSIX and Windows branches use `os.lstat` |
| `tests/auth/test_store.py::test_symlink_attack_rejected` | `AuthVaultSymlinkError` | `pytest.raises(AuthVaultSymlinkError)` | WIRED | Replaced by 6 STORE-22 tests; pattern `pytest.raises(s.AuthVaultSymlinkError)` at lines :513, :518, :540, :562, :586 |
| `cli_ops.py::login` | `refresh.py::with_vault_lock` | `with with_vault_lock(vault_path): vault = load_vault(...); ...; save_vault(...)` | WIRED | `cli_ops.py:276`; import at `:28` |
| `cli_ops.py::logout` | `refresh.py::with_vault_lock` | same wrapper | WIRED | `cli_ops.py:325` |
| `refresh.py::with_vault_lock` | `filelock.FileLock` (sync sibling of AsyncFileLock) | `filelock.FileLock(str(_lock_path_for(vault_path)), timeout=LOCK_TIMEOUT_SECONDS)` | WIRED | `refresh.py:212-216` constructs FileLock with same lock_path as the async sibling |

### Requirements Coverage

| Requirement | Source Plan(s) | Description | Status | Evidence |
| ----------- | -------------- | ----------- | ------ | -------- |
| AUTH-06 | 022.2-01, 022.2-02 | `.state/auth.json` created chmod 0600 via `os.open(..., 0o600)` + `os.fchmod`; chmod verified on every read | SATISFIED | The chmod 0600 invariant preserved AND now hardened with O_NOFOLLOW (Plan 01) and concurrent-write protection via filelock (Plan 02). `_atomic_write` opens with VAULT_MODE=0o600 + O_NOFOLLOW; `_verify_mode` uses `os.lstat` to refuse symlink redirection of the mode check; concurrent writers serialize on `<vault>.lock`. REQUIREMENTS.md line 14 already marks `[x]` complete from Phase 012; this phase strengthens the invariant (defense-in-depth) without breaking it. |

REQUIREMENTS.md cross-check: AUTH-06 is the only requirement listed in either plan's frontmatter for Phase 022.2. No orphaned requirements detected (the phase scope is purely defense-in-depth on already-marked-complete AUTH-06).

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| (none) | — | No `@pytest.mark.skip` regression introduced; no TODO/FIXME/PLACEHOLDER markers in modified files; no `console.log`-style stubs; no empty handlers | — | Clean |

Note: 4 INFO-level observations were captured by the `022.2-REVIEW.md` code review (advisory only — residual TOCTOU notes outside the threat-model scope of "non-adversarial local FS"). Per CONTEXT.md / threat-model, these are acceptably scoped and do NOT constitute blockers.

## Step 7b: Quality Findings

Skipped (quality.level: fast)

## Human Verification Required

None — all 15 must-haves verified programmatically via:
- File-content grep for required tokens (O_NOFOLLOW, os.lstat, AuthVaultSymlinkError, with_vault_lock)
- Import-graph trace (cli_ops → refresh, tests → store)
- Test-baseline confirmation (full suite 772 passed / 0 failed; auth suite 412 passed / 0 failed)
- Code-review independent confirmation (`022.2-REVIEW.md` status: `issues_found` advisory, 0 MUST/SHOULD FIX)

The two threats (T-018-8 + T-018-9) are now CLOSED with regression tests in place. No human-in-the-loop validation needed.

## Gaps Summary

No gaps. All 15 must-haves across both plans are verified. Both deferred LOW threats from Phase 018 are CLOSED:

- **T-018-9 (symlink attack):** O_NOFOLLOW on tmp-file open + lstat at vault path / tmp path / parent dir / ensure_initialized; `AuthVaultSymlinkError` exception with hygienic message (no link-target leakage); 6 STORE-22 tests covering vault-path symlink, tmp-path symlink, parent-dir symlink, ensure_initialized rejection, message hygiene, and positive control.
- **T-018-8 (vault race):** sync `with_vault_lock` context manager wrapping the same `<vault>.lock` POSIX advisory lock as Phase 013's async refresh; both `cli_ops.login()` and `cli_ops.logout()` hold it across `load_vault → mutate → save_vault`; `_emit_auth_event` correctly placed OUTSIDE the lock; concurrent N=4 ThreadPoolExecutor regression tests pass deterministically.

The v2 milestone audit's "deferred LOW threats" tech-debt category is now empty. Recommend re-running `/gsd:audit-milestone v2` to flip the category in the next audit cycle.

---

_Verified: 2026-05-02_
_Verifier: Claude (gsd-verifier)_
