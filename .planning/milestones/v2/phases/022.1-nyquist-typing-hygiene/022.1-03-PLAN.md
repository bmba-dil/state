---
phase: 022.1-nyquist-typing-hygiene
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/state_core/py.typed
autonomous: true
requirements: []
gap_closure: true

must_haves:
  truths:
    - "`src/state_core/py.typed` exists as a zero-byte file"
    - "mypy run against `src/state_core/` no longer emits `[import-untyped]` warnings caused by the absent state_core marker (third-party package warnings are out of scope and may persist)"
    - "PEP 561 inline-type-information convention satisfied for the state_core package"
  artifacts:
    - path: "src/state_core/py.typed"
      provides: "PEP 561 inline-type-information marker — declares state_core as type-checked package"
      min_lines: 0
  key_links:
    - from: "downstream consumers running mypy on code that imports `state_core.*`"
      to: "src/state_core/py.typed"
      via: "PEP 561 marker discovery during mypy package walk"
      pattern: "py.typed"
---

<objective>
Add an empty `py.typed` marker file to `src/state_core/` per PEP 561 so that mypy treats `state_core` as a type-checked package and stops emitting `[import-untyped]` warnings against state_core imports. This was surfaced as a project-wide tech-debt item in `v2-MILESTONE-AUDIT.md` § Tech Debt Summary, originally observed in Phase 011 + Phase 019 caveats.

Purpose: Eliminate noisy mypy false-positives that affect every developer running `mypy` on auth modules. The fix is a single zero-byte file. Per PEP 561, the file must be empty (no content) — its mere presence is the declaration.

Output: `src/state_core/py.typed` (0 bytes), committed.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v2-MILESTONE-AUDIT.md
@src/state_core/__init__.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create empty py.typed marker in src/state_core/</name>
  <files>src/state_core/py.typed</files>
  <read_first>
    - `src/state_core/__init__.py` (confirm package layout and that `src/state_core/` is the canonical package root)
    - `.planning/v2-MILESTONE-AUDIT.md` § Tech Debt Summary (confirms project-wide py.typed item is what this task closes)
  </read_first>
  <action>
    Create a zero-byte file at exactly `src/state_core/py.typed`. PEP 561 requires the file to exist with no content (its presence is the type-information declaration; any content is irrelevant but conventionally empty).

    Concrete commands (any one of these is sufficient):
    ```bash
    : > src/state_core/py.typed
    ```
    or:
    ```bash
    touch src/state_core/py.typed
    ```

    Use the Write tool with empty content `""` if `: >` / `touch` are not preferred. Do NOT add any text, comments, or version markers — the file MUST be 0 bytes for PEP 561 conformance and to match every site-packages reference (`structlog/py.typed`, `pygit2/py.typed`, etc., are all 0 bytes).

    Do NOT create py.typed in any other package (e.g., not in `state_build/`, `state_teach/`, or `state_daemon/`) — this audit item is scoped to `state_core` per the audit's exact wording. Other packages are separate concerns out of scope.

    <quality_scan>
      <code_to_reuse>
        - Known: `.venv/lib/python3.12/site-packages/structlog/py.typed`, `pygit2/py.typed`, `attrs/py.typed` — all are 0-byte files; confirms convention via `wc -c` on any of them
        - Grep pattern: `find .venv/lib/python3.12/site-packages -name "py.typed" -size 0c | head -3` — confirms 0-byte convention used by upstream packages
        - Grep pattern: `find src -name "py.typed" 2>/dev/null` — confirms no existing markers in our packages (avoid double-creation)
      </code_to_reuse>
      <docs_to_consult>
        - Description: PEP 561 — Distributing and Packaging Type Information. Section "Packaging Type Information" specifies the empty `py.typed` marker convention. No external library; this is a Python ecosystem standard.
      </docs_to_consult>
      <tests_to_write>
        N/A — no exported logic; the file IS the artifact; verification is `test -f` + `wc -c == 0`
      </tests_to_write>
    </quality_scan>
  </action>
  <verify>
    <automated>test -f src/state_core/py.typed && [ "$(wc -c < src/state_core/py.typed | tr -d ' ')" = "0" ]</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/state_core/py.typed` exits 0
    - `wc -c < src/state_core/py.typed` outputs `0` (exact zero bytes — no trailing newline, no BOM)
    - `find src -name "py.typed"` lists exactly one path: `src/state_core/py.typed`
    - File is tracked by git (`git ls-files src/state_core/py.typed` returns the path after staging)
  </acceptance_criteria>
  <done>Empty py.typed marker exists at src/state_core/py.typed; PEP 561 convention satisfied for state_core package.</done>
</task>

<task type="auto">
  <name>Task 2: Smoke-check mypy no longer warns about state_core itself</name>
  <files>(none — read-only verification)</files>
  <read_first>
    - `src/state_core/py.typed` (must exist from Task 1)
    - `src/state_core/__init__.py` (mypy entry point)
    - `pyproject.toml` (confirm mypy config / no overrides that would mask the result)
  </read_first>
  <action>
    Run mypy against `src/state_core/` and assert that no `[import-untyped]` warnings target the `state_core` package itself. Third-party package warnings (e.g., `[import-untyped]` against external libs that lack their own py.typed) are out of scope and acceptable.

    Concrete invocation:
    ```bash
    python3 -m mypy src/state_core/ 2>&1 | tee /tmp/mypy-state_core.log
    ```

    Then check the log for state_core-attributed import-untyped warnings. The PEP 561 fix means lines like:
    ```
    src/something_else/foo.py:N: error: Skipping analyzing "state_core.auth": module is installed, but missing library stubs or py.typed marker  [import-untyped]
    ```
    must NOT appear.

    Verification command (acceptable result is zero matches):
    ```bash
    grep -E '"state_core(\.[a-z_]+)*".*\[import-untyped\]' /tmp/mypy-state_core.log
    ```
    Expected: empty output, exit code 1.

    If mypy itself fails to install or run (e.g., missing mypy in the environment), this is acceptable — log the issue in the SUMMARY and treat py.typed presence (Task 1 acceptance) as the binding deliverable. The mypy smoke is a confidence check, not a blocking gate, because mypy availability is environment-dependent and orthogonal to PEP 561 conformance.

    Do NOT attempt to fix any unrelated mypy errors uncovered by this run — they are out of scope for Phase 022.1. Record them in the SUMMARY for future cleanup phases.

    <quality_scan>
      <code_to_reuse>
        - Known: `pyproject.toml` mypy config (if present) — `grep -A 20 "\[tool.mypy\]" pyproject.toml`
        - Grep pattern: `which mypy || python3 -m mypy --version` — confirm tool availability before running
        - Grep pattern: `python3 -m pip show mypy 2>/dev/null | head -3` — version sanity check
      </code_to_reuse>
      <docs_to_consult>
        - Description: PEP 561 § "Type Checker Module Resolution Order" — explains that py.typed silences `[import-untyped]` for the marked package only; downstream third-party warnings are independent and unaffected.
      </docs_to_consult>
      <tests_to_write>
        N/A — verification command is the test
      </tests_to_write>
    </quality_scan>
  </action>
  <verify>
    <automated>test -f src/state_core/py.typed && (python3 -m mypy src/state_core/ 2>&1 | grep -E '"state_core(\.[a-z_]+)*".*\[import-untyped\]' | head -1; [ ${PIPESTATUS[1]} -eq 1 ] || [ -z "$(python3 -m mypy src/state_core/ 2>&1 | grep -E '\"state_core(\.[a-z_]+)*\".*\[import-untyped\]')" ])</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -m mypy src/state_core/ 2>&1 | grep -cE '"state_core(\.[a-z_]+)*".*\[import-untyped\]'` returns `0` (zero state_core-attributed import-untyped warnings)
    - If mypy is unavailable in the environment, this acceptance auto-passes (record in SUMMARY); Task 1's `test -f` + `wc -c == 0` is the binding gate
    - No new files created by this task
  </acceptance_criteria>
  <done>mypy confirms state_core py.typed marker is recognized (or mypy unavailable and recorded as such); no state_core-attributed [import-untyped] warnings remain.</done>
</task>

</tasks>

<verification>
```bash
# Marker exists and is empty
test -f src/state_core/py.typed
wc -c < src/state_core/py.typed   # → 0

# No state_core-attributed import-untyped from mypy
python3 -m mypy src/state_core/ 2>&1 | grep -cE '"state_core(\.[a-z_]+)*".*\[import-untyped\]'   # → 0
```
</verification>

<success_criteria>
- `src/state_core/py.typed` exists at exactly 0 bytes
- mypy on `src/state_core/` reports zero `[import-untyped]` warnings attributed to the state_core package itself (third-party warnings out of scope)
- No other packages affected (no py.typed accidentally created in state_build/state_teach/state_daemon)
</success_criteria>

<output>
After completion, create `.planning/milestones/v2/phases/022.1-nyquist-typing-hygiene/022.1-03-SUMMARY.md` documenting:
- Confirmation `wc -c src/state_core/py.typed` returned `0`
- mypy smoke result (zero state_core-attributed import-untyped warnings, OR mypy unavailable)
- Any unrelated mypy errors observed (recorded for future cleanup, NOT fixed in this phase)
</output>
