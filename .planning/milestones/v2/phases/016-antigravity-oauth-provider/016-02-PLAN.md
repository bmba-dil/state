---
phase: "016"
plan: "02"
type: execute
wave: 2
depends_on: ["016-01"]
files_modified:
  - src/state_core/auth/providers/antigravity.py
autonomous: true
requirements: [AUTH-03]

must_haves:
  truths:
    - "src/state_core/auth/providers/antigravity.py module imports cleanly (no NameError, no circular import)"
    - "All 8 Wave-2 tests in test_antigravity.py turn from XFAIL → GREEN"
    - "Constants are PLAINTEXT (P1-3) — _CLIENT_ID + _CLIENT_SECRET are bare string literals with rationale comment block citing P1-3"
    - "Pydantic AntigravityTokenResponse model validates real Google token-endpoint responses with extra='ignore' for forward-compat"
    - "Sync helpers (_parse_id_token_payload, _to_credential, _build_authorize_url, _platform_for_client_metadata, _build_client_metadata, _exchange_code) are COMPLETE"
  artifacts:
    - path: "src/state_core/auth/providers/antigravity.py"
      provides: "Module-level constants, Pydantic models, all 6 sync helpers"
      min_lines: 280
      contains: "_CLIENT_ID, _CLIENT_SECRET, _REDIRECT_PORT, _REDIRECT_HOST_LITERAL, _SCOPES, _USER_AGENT_LITERAL, _X_GOOG_API_CLIENT, AntigravityTokenResponse, _parse_id_token_payload, _to_credential, _build_authorize_url, _platform_for_client_metadata, _build_client_metadata, _exchange_code"
  key_links:
    - from: "src/state_core/auth/providers/antigravity.py"
      to: "src/state_core/auth/oauth_common/loopback.py"
      via: "import statement at module top"
      pattern: "from state_core.auth.oauth_common.loopback import"
    - from: "src/state_core/auth/providers/antigravity.py"
      to: "src/state_core/auth/errors.py"
      via: "import statement at module top"
      pattern: "from state_core.auth.errors import"
    - from: "src/state_core/auth/providers/antigravity.py"
      to: "src/state_core/auth/oauth_common/pkce.py"
      via: "import statement at module top"
      pattern: "from state_core.auth.oauth_common.pkce import"

threat_model:
  asvs_l1: "Plan 02 establishes constants + URL building + token-exchange helpers. Threat surface: client_secret embedded in source (mitigated via P1-3 rationale comment + Desktop-App OAuth threat model — security boundary is PKCE+loopback NOT secret confidentiality), id_token spoofing (mitigated by TLS to oauth2.googleapis.com + payload-only parse with no signature verification — Pitfall 8 / RESEARCH rule 5), authorize URL parameter tampering (mitigated by CSRF state + PKCE challenge orthogonal validation in Wave 4)."
  controls:
    - "P1-3 rationale comment block above _CLIENT_SECRET line (~12 lines) cites .planning/research/PITFALLS.md and explains AV-scanner false-positive risk"
    - "AntigravityTokenResponse uses Field(repr=False) on access_token AND refresh_token AND id_token (all secrets/PII)"
    - "_parse_id_token_payload raises AuthLoginError on malformed JWT — caller can surface remediation"
    - "_exchange_code uses per-call httpx.AsyncClient with Timeout(10.0, connect=5.0) and follow_redirects=False — DoS-by-redirect mitigation"
    - "OAuth traffic NEVER routes through litellm — direct httpx.AsyncClient (CLAUDE.md cardinal rule)"
    - "No filelock acquisition in module — Phase 013's refresh_credential owns the lock (refresh.py rule 9 — deadlock prevention)"
---

<objective>
Land the constants block, Pydantic models, and 6 sync helpers for `src/state_core/auth/providers/antigravity.py`. Mirror Phase 015's `google_gemini.py` structure section-for-section with the surgical Antigravity diffs (different `_CLIENT_ID`/`_CLIENT_SECRET`, FIXED port 51121, literal `localhost`, 5 scopes, Antigravity-specific outbound headers via `_platform_for_client_metadata` + `_build_client_metadata`).

Purpose: Wave 2 turns 8 tests GREEN: `test_constants_plaintext`, `test_build_authorize_url`, `test_localhost_literal_in_redirect_uri`, `test_fixed_port_51121`, `test_five_scopes_present`, `test_id_token_parser`, `test_exchange_code`, `test_client_metadata_platform_detection`. Plan 03 lands the `AntigravityAuth` class methods. Plan 04 fills the async login/refresh bodies.

Output: `src/state_core/auth/providers/antigravity.py` (~290 LOC of source-without-class). The class skeleton is added by Plan 03.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/milestones/v2/phases/016-antigravity-oauth-provider/016-RESEARCH.md
@.planning/milestones/v2/phases/016-antigravity-oauth-provider/016-VALIDATION.md
@.planning/milestones/v2/phases/016-antigravity-oauth-provider/016-01-PLAN.md
@.planning/research/PITFALLS.md
@src/state_core/auth/providers/google_gemini.py
@src/state_core/auth/oauth_common/loopback.py
@src/state_core/auth/oauth_common/pkce.py
@src/state_core/auth/errors.py
@src/state_core/auth/base.py
@src/state_core/auth/refresh.py
@tests/auth/providers/test_antigravity.py

<interfaces>
From src/state_core/auth/oauth_common/pkce.py (Phase 014):
  generate_verifier() -> str  # 43-char base64url-no-pad
  build_challenge(verifier: str) -> str

From src/state_core/auth/oauth_common/loopback.py (Phase 015):
  SIGN_IN_SUCCESS_URL: str
  SIGN_IN_FAILURE_URL: str
  allocate_loopback_port() -> int  # IMPORT but DO NOT CALL
  async wait_for_oauth_callback(port, *, expected_state, timeout=300.0) -> str

From src/state_core/auth/errors.py (Phase 015):
  AuthError, AuthLoginError, AuthRefreshError

From src/state_core/auth/refresh.py (Phase 013):
  is_expired_buffered(cred, now: float) -> bool  # 5-min buffer P0-7

From src/state_core/auth/base.py (Phase 011):
  OAuthCredential(BaseModel, frozen=True, extra="forbid"):
    access, refresh, expires (epoch), provider_id, account_id, extras
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Constants + imports + Pydantic models (header through line ~200)</name>
  <files>src/state_core/auth/providers/antigravity.py</files>
  <read_first>
    - src/state_core/auth/providers/google_gemini.py (lines 1-200 — line-for-line template)
    - .planning/milestones/v2/phases/016-antigravity-oauth-provider/016-RESEARCH.md (Code Examples Section 1 + 2)
    - .planning/research/PITFALLS.md (P1-3 rationale)
    - tests/auth/providers/test_antigravity.py (the Wave-2 tests this task must satisfy)
  </read_first>
  <behavior>
    - test_constants_plaintext: `_CLIENT_ID == "1071006060591-tmhssin2h21lcre235vtolojh4g403ep.apps.googleusercontent.com"` literal, `_CLIENT_SECRET == "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"` literal. Neither reads `os.environ`. No base64 decode at module load.
    - test_fixed_port_51121: `_REDIRECT_PORT == 51121`, `_REDIRECT_HOST_LITERAL == "localhost"`, `_REDIRECT_PATH == "/oauth-callback"`. `allocate_loopback_port` imported but NEVER CALLED in module.
    - test_five_scopes_present: `_SCOPES` is a single space-separated string containing all 5 scope URLs (cloud-platform, userinfo.email, userinfo.profile, cclog, experimentsandconfigs).
    - AntigravityTokenResponse Pydantic model validates dict with access_token, expires_in, optional refresh_token + id_token + scope + token_type, with extra="ignore".
  </behavior>
  <action>
    Create `src/state_core/auth/providers/antigravity.py`. Mirror google_gemini.py lines 1-200 with surgical swaps. Write in this order:

    1. **Module docstring** verbatim from RESEARCH.md Code Examples Section 1 (the docstring beginning `"""Antigravity (Google Cloud Code Assist Companion) OAuth provider.`). Include the ToS warning paragraph and the cardinal-rules-deltas list.

    2. **Imports block**, copying google_gemini.py lines 71-112 with these adjustments:
       - Keep `print = print  # noqa: A001` re-bind for testability
       - Import `AuthMethod, Credential, OAuthCredential` from `state_core.auth.base`
       - Import `AuthError, AuthLoginError, AuthRefreshError` from `state_core.auth.errors`
       - Import `is_expired_buffered` from `state_core.auth.refresh`
       - Import `build_challenge, generate_verifier` from `state_core.auth.oauth_common.pkce` (noqa F401 — re-export)
       - Import `SIGN_IN_FAILURE_URL, SIGN_IN_SUCCESS_URL, allocate_loopback_port, wait_for_oauth_callback` from `state_core.auth.oauth_common.loopback` (noqa F401 — `allocate_loopback_port` imported for test surface but NEVER CALLED in module)
       - `log = structlog.get_logger(__name__)`

    3. **Identity constants block** verbatim from RESEARCH.md Code Examples Section 2:
       - 12-line P1-3 rationale comment (cites NoeFabris source, PicoClaw docs, taoalpha gist, dated 2026-04-30)
       - `_CLIENT_ID: str = "1071006060591-tmhssin2h21lcre235vtolojh4g403ep.apps.googleusercontent.com"` (verbatim)
       - `_CLIENT_SECRET: str = "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"` (verbatim plaintext)

    4. **URL constants block**:
       - `_AUTHORIZE_URL: str = "https://accounts.google.com/o/oauth2/v2/auth"`
       - `_TOKEN_URL: str = "https://oauth2.googleapis.com/token"`

    5. **Loopback redirect block** with comments citing Pitfall 4 + Pitfall 5:
       - `_REDIRECT_HOST_LITERAL: str = "localhost"`  (NOT 127.0.0.1)
       - `_REDIRECT_PORT: int = 51121`  (NOT kernel-allocated)
       - `_REDIRECT_PATH: str = "/oauth-callback"`

    6. **Scopes block** with comment citing P2-3 + Google discovery doc URL:
       - `_SCOPES` as single space-separated 5-scope string in the order: cloud-platform, userinfo.email, userinfo.profile, cclog, experimentsandconfigs

    7. **Antigravity outbound headers block**:
       - `_USER_AGENT_LITERAL: str = "antigravity"` (literal, NOT version-suffixed — comment cites Pitfall 6 + RESEARCH alternative)
       - `_X_GOOG_API_CLIENT: str = "google-cloud-sdk vscode_cloudshelleditor/0.1"`
       - Comment: "Client-Metadata is built per-call so sys.platform monkeypatch works in tests."

    8. **Pydantic models block** (~50 lines):
       - `AntigravityTokenResponse(BaseModel)` with `model_config = ConfigDict(extra="ignore")`. Fields: `access_token: str = Field(repr=False)`, `expires_in: int`, `refresh_token: str | None = Field(default=None, repr=False)`, `scope: str | None = None`, `token_type: str | None = None`, `id_token: str | None = Field(default=None, repr=False)`. Docstring cites P2-2 rotation contract.
       - `_GoogleIdTokenPayload(BaseModel)` with `model_config = ConfigDict(extra="ignore")`. Fields: `sub: str`, `email: str | None = None`, `email_verified: bool | None = None`. Docstring cites Pitfall 8 (no signature verification — TLS trust boundary).

    Stop here. Task 2 adds helpers; Task 3 adds final wiring + __all__.

    <quality_scan>
      <code_to_reuse>
        - Known: `src/state_core/auth/providers/google_gemini.py` lines 1-200 — line-for-line template
        - Grep pattern: `grep -n "^_CLIENT_ID\|^_CLIENT_SECRET\|^_REDIRECT\|^_SCOPES\|^_USER_AGENT" src/state_core/auth/providers/google_gemini.py`
      </code_to_reuse>
      <docs_to_consult>
        - Context7: `/pydantic/pydantic` — query "ConfigDict extra='ignore' Field repr=False"
        - Description: orjson for deterministic JSON key order in Task 2's _build_client_metadata
      </docs_to_consult>
      <tests_to_write>
        - File: `tests/auth/providers/test_antigravity.py` (already created in Plan 01)
        - What to test: `test_constants_plaintext`, `test_fixed_port_51121`, `test_five_scopes_present` flip XFAIL → GREEN. Remove the `pytest.xfail("Plan 02 …")` line at the head of each test; keep the assertion body intact.
      </tests_to_write>
    </quality_scan>
  </action>
  <verify>
    <automated>cd /Users/tmac/Projects/state && PYTHONPATH=$PWD/src .venv/bin/python -c "from state_core.auth.providers import antigravity as a; assert a._CLIENT_ID == '1071006060591-tmhssin2h21lcre235vtolojh4g403ep.apps.googleusercontent.com'; assert a._CLIENT_SECRET == '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'; assert a._REDIRECT_PORT == 51121; assert a._REDIRECT_HOST_LITERAL == 'localhost'; assert 'cclog' in a._SCOPES and 'experimentsandconfigs' in a._SCOPES; assert a._USER_AGENT_LITERAL == 'antigravity'; assert a.AntigravityTokenResponse(access_token='ya29.x', expires_in=3599).access_token == 'ya29.x'; print('OK')"</automated>
  </verify>
  <done>
    Module imports cleanly with no errors. All 8 constants present with verbatim values from RESEARCH.md. AntigravityTokenResponse + _GoogleIdTokenPayload models validate. P1-3 rationale comment block (≥10 lines) present. `grep -c "allocate_loopback_port(" src/state_core/auth/providers/antigravity.py` returns 0 (imported but never called). `grep -c "P1-3" src/state_core/auth/providers/antigravity.py` ≥ 1.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Sync helpers — _parse_id_token_payload, _to_credential, _build_authorize_url, _platform_for_client_metadata, _build_client_metadata, _exchange_code</name>
  <files>src/state_core/auth/providers/antigravity.py</files>
  <read_first>
    - src/state_core/auth/providers/google_gemini.py (lines 200-376 — helpers section, line-for-line template)
    - .planning/milestones/v2/phases/016-antigravity-oauth-provider/016-RESEARCH.md (Patterns 2, 3, 4, 5 + Code Examples Sections 3, 4, 5, 6)
    - tests/auth/providers/test_antigravity.py (Wave-2 tests for helpers)
  </read_first>
  <behavior>
    - test_id_token_parser: `_parse_id_token_payload(<3-part JWT>)` returns object with `sub` and `email`. Bad JWT (2 parts or invalid base64) raises `AuthLoginError`. Same logic as Phase 015.
    - test_build_authorize_url: Returned URL contains `client_id=1071006060591-…`, `code_challenge_method=S256`, `access_type=offline`, `prompt=consent`, `response_type=code`, `scope=` containing all 5 scopes.
    - test_localhost_literal_in_redirect_uri: When `redirect_uri = f"http://localhost:51121/oauth-callback"`, the urlencoded result contains `localhost%3A51121%2Foauth-callback` (or its variants) and does NOT contain `127.0.0.1`.
    - test_exchange_code: Form-urlencoded POST to `_TOKEN_URL` with body containing all 6 fields including `client_secret=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`. Returns parsed `AntigravityTokenResponse`. 4xx raises `AuthLoginError` with status + body[:500] in message.
    - test_client_metadata_platform_detection: `_platform_for_client_metadata()` returns "MACOS" for `sys.platform == "darwin"`, "LINUX" for `linux*`, "WINDOWS" for `win*`. `_build_client_metadata("MACOS")` returns valid JSON with exactly 3 keys (`ideType=ANTIGRAVITY`, `platform=MACOS`, `pluginType=GEMINI`).
  </behavior>
  <action>
    Append the 6 helpers to `src/state_core/auth/providers/antigravity.py` AFTER the Pydantic models block from Task 1.

    1. **`_parse_id_token_payload(id_token: str) -> _GoogleIdTokenPayload`**: Copy verbatim from `google_gemini.py` lines 206-238. Splits JWT on `.`, decodes middle segment with `base64.urlsafe_b64decode` (with `=` padding), parses with `orjson.loads`, validates with `_GoogleIdTokenPayload.model_validate(payload_json)`. Raises `AuthLoginError` for: not 3 parts, bad base64, bad JSON, ValidationError.

    2. **`_to_credential(resp: AntigravityTokenResponse, *, original_refresh: str, now: float) -> OAuthCredential`**: Copy from `google_gemini.py` lines 241-289 with ONE swap: `provider_id="google.antigravity"` (not `"google.gemini_cli"`). Logic:
       - If `resp.id_token`: parse via `_parse_id_token_payload`; populate `account_id = payload.sub`; populate `extras["email"]` and `extras["email_verified"]` if present
       - Else: `account_id = None`, `extras = {}`
       - Return `OAuthCredential(access=resp.access_token, refresh=resp.refresh_token or original_refresh, expires=now + float(resp.expires_in), provider_id="google.antigravity", account_id=account_id, extras=extras)`
       - Docstring cites P2-2 rotation rule + Phase 011 wire-shape invariant (no buffer subtracted at storage).

    3. **`_build_authorize_url(redirect_uri: str, state: str, challenge: str) -> str`**: Copy from `google_gemini.py` lines 292-328. Use `urlencode` with these keys (order matters for golden-file test stability):
       ```
       client_id, redirect_uri, response_type=code, scope=_SCOPES,
       state, code_challenge, code_challenge_method=S256,
       access_type=offline, prompt=consent
       ```
       Docstring cites: state ≠ verifier (RFC 6749 §10.12 + RFC 7636), `access_type=offline` mandatory for refresh token issuance, `prompt=consent` forces refresh token on repeat logins. Returns `f"{_AUTHORIZE_URL}?{qs}"`.

    4. **`_platform_for_client_metadata() -> str`**: New helper (no Phase 015 precedent). From RESEARCH.md Code Examples Section 5:
       ```python
       def _platform_for_client_metadata() -> str:
           """Map sys.platform → Client-Metadata platform value.

           Antigravity backend pattern-matches on uppercase MACOS/LINUX/WINDOWS.
           Computed per-call so tests can monkeypatch sys.platform across all
           three branches.
           """
           p = sys.platform
           if p == "darwin":
               return "MACOS"
           if p.startswith("linux"):
               return "LINUX"
           if p.startswith("win"):
               return "WINDOWS"
           return "LINUX"  # defensive fallback
       ```

    5. **`_build_client_metadata(platform: str) -> str`**: New helper. From RESEARCH.md Code Examples Section 5:
       ```python
       def _build_client_metadata(platform: str) -> str:
           """Compose the Client-Metadata header value as deterministic JSON.

           orjson default key order is insertion-order; we explicitly insert
           ideType → platform → pluginType to match the Antigravity-IDE wire format.
           """
           return orjson.dumps(
               {"ideType": "ANTIGRAVITY", "platform": platform, "pluginType": "GEMINI"},
           ).decode("ascii")
       ```

    6. **`async _exchange_code(code: str, verifier: str, redirect_uri: str) -> AntigravityTokenResponse`**: Copy from `google_gemini.py` lines 331-376. Body keys (form-urlencoded): `code, client_id=_CLIENT_ID, client_secret=_CLIENT_SECRET, redirect_uri, grant_type="authorization_code", code_verifier=verifier`. Per-call `httpx.AsyncClient(Timeout(10.0, connect=5.0), follow_redirects=False)`. POST `_TOKEN_URL` with `data=body` and `headers={"accept": "application/json"}`. Error precedence: HTTPError → `AuthLoginError(f"token exchange transport error: {exc}")`; status >= 400 → `AuthLoginError(f"token exchange http {status}: {body[:500]!r}")`; ValidationError → `AuthLoginError(f"token exchange response shape: {exc}")`. Returns `AntigravityTokenResponse.model_validate_json(resp.content)`.

    <quality_scan>
      <code_to_reuse>
        - Known: `src/state_core/auth/providers/google_gemini.py` lines 200-376 — line-for-line template for 4 of the 6 helpers (only `_platform_for_client_metadata` and `_build_client_metadata` are net-new)
        - Grep pattern: `grep -n "^def _\|^async def _" src/state_core/auth/providers/google_gemini.py`
      </code_to_reuse>
      <docs_to_consult>
        - Context7: `/encode/httpx` — query "AsyncClient form-urlencoded data parameter Timeout follow_redirects=False"
        - Context7: `/ijl/orjson` — query "dumps insertion-order JSON deterministic key ordering"
      </docs_to_consult>
      <tests_to_write>
        - File: `tests/auth/providers/test_antigravity.py`
        - What to test: `test_id_token_parser`, `test_build_authorize_url`, `test_localhost_literal_in_redirect_uri`, `test_exchange_code`, `test_client_metadata_platform_detection` flip XFAIL → GREEN. Remove `pytest.xfail("Plan 02 ...")` lines.
      </tests_to_write>
    </quality_scan>
  </action>
  <verify>
    <automated>cd /Users/tmac/Projects/state && PYTHONPATH=$PWD/src .venv/bin/python -m pytest tests/auth/providers/test_antigravity.py::test_id_token_parser tests/auth/providers/test_antigravity.py::test_build_authorize_url tests/auth/providers/test_antigravity.py::test_localhost_literal_in_redirect_uri tests/auth/providers/test_antigravity.py::test_exchange_code tests/auth/providers/test_antigravity.py::test_client_metadata_platform_detection -x -q</automated>
  </verify>
  <done>
    All 5 helper tests GREEN. Combined with Task 1's 3 constant tests = 8 Wave-2 tests GREEN. `grep -c "^def _parse_id_token_payload\|^def _to_credential\|^def _build_authorize_url\|^def _platform_for_client_metadata\|^def _build_client_metadata\|^async def _exchange_code" src/state_core/auth/providers/antigravity.py` returns 6. No regression in Phase 014 + Phase 015 tests. The string `provider_id="google.antigravity"` appears verbatim in `_to_credential`. The literal `"localhost"` appears in helper code (NOT `127.0.0.1` — verified by `grep -c "127.0.0.1" src/state_core/auth/providers/antigravity.py` returning 0).
  </done>
</task>

</tasks>

<verification>
- All 8 Wave-2 tests in test_antigravity.py GREEN: test_constants_plaintext, test_build_authorize_url, test_localhost_literal_in_redirect_uri, test_fixed_port_51121, test_five_scopes_present, test_id_token_parser, test_exchange_code, test_client_metadata_platform_detection
- Phase 014 + Phase 015 + Wave-3/Wave-4 XFAIL tests still pass (no regression)
- `grep -c "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" src/state_core/auth/providers/antigravity.py` returns 1 (verbatim plaintext, NOT base64/XOR)
- `grep -c "1071006060591-tmhssin2h21lcre235vtolojh4g403ep" src/state_core/auth/providers/antigravity.py` returns ≥ 1
- `grep -c "P1-3" src/state_core/auth/providers/antigravity.py` ≥ 1 (rationale comment)
- `grep -c "127.0.0.1" src/state_core/auth/providers/antigravity.py` returns 0 (Pitfall 5)
- `grep -c "import litellm\|from litellm" src/state_core/auth/providers/antigravity.py` returns 0 (CLAUDE.md cardinal rule)
- `grep -c "from filelock\|AsyncFileLock\|import filelock" src/state_core/auth/providers/antigravity.py` returns 0 (refresh.py rule 9)
- `grep -c "allocate_loopback_port(" src/state_core/auth/providers/antigravity.py` returns 0 (imported but never called — Pitfall 4)
</verification>

<success_criteria>
- [ ] `src/state_core/auth/providers/antigravity.py` exists, ≥ 280 LOC at end of plan
- [ ] All 8 Wave-2 tests in test_antigravity.py GREEN
- [ ] All Wave-3/Wave-4 tests still XFAIL (Plan 03/04 unblocked)
- [ ] Constants verbatim from RESEARCH.md: `_CLIENT_ID`, `_CLIENT_SECRET`, `_REDIRECT_PORT=51121`, `_REDIRECT_HOST_LITERAL="localhost"`, `_SCOPES` with all 5 scope URLs, `_USER_AGENT_LITERAL="antigravity"`, `_X_GOOG_API_CLIENT="google-cloud-sdk vscode_cloudshelleditor/0.1"`
- [ ] P1-3 rationale comment block present (≥10 lines)
- [ ] No `127.0.0.1`, no litellm import, no filelock import, no `allocate_loopback_port()` call
</success_criteria>

<output>
After completion, create `.planning/milestones/v2/phases/016-antigravity-oauth-provider/016-02-SUMMARY.md` documenting:
- 6 sync helpers landed (signatures + 1-line description each)
- All 8 Wave-2 test row IDs flipped XFAIL → GREEN
- Cardinal-rule grep gates passing (P1-3 plaintext, no 127.0.0.1, no litellm, no filelock, no allocate_loopback_port call)
- Files modified summary + commit hashes
- Plan 03 readiness — `AntigravityAuth` class and sync methods (is_token, is_expired, http_headers) ready to land
</output>
