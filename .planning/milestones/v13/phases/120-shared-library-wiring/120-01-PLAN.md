---
phase: 120
plan: 01
type: auto
autonomous: true
plan_name: "state_teach → state_core wiring (auth, events)"
requirements: []
gates: 0
---

<objective>
Wire state_teach/mcp.py to the shared state_core library for auth (provider credentials
via load_credentials) and events (event store via SqliteEventStore), following the
identical pattern established by state_build/mcp.py Phase 111. Add a test verifying
the wiring works, and run import-lint to confirm no cross-mode violations.
</objective>

<context>
Part of Phase 120: "Shared library wiring (auth, events via state_core)"
Goal: As 111 (auth, events via state_core shared library wiring).
Depends on: 115 (MCP server scaffold — already done).

state_build/mcp.py already imports from state_core.auth and state_core.events using a
specific pattern (aliased imports with noqa: F401 comments). This plan mirrors that
pattern in state_teach/mcp.py.

Mode isolation rule (CLAUDE.md): state_teach.* must not import state_build.*.
Importing state_core.* is allowed — it's the shared kernel.
</context>

<tasks>

<task number="1" type="auto" done="false">
<description>Wire state_teach/mcp.py to state_core.auth (load_credentials) and state_core.events (SqliteEventStore)</description>
<implementation>

Add two import lines to `src/state_teach/mcp.py` after the existing `state_core.schema` import,
mirroring the state_build pattern:

```python
from state_core.auth import load_credentials as _load_credentials  # noqa: F401
from state_core.events import SqliteEventStore as _SqliteEventStore  # noqa: F401
```

The imports should follow immediately after line 14 (`from state_core.schema import validate_mode_config`).
Also import Context from mcp.server.fastmcp (removing the # noqa comment since it'll now be used).
</implementation>

<verification>
1. Imports resolve without error: `python3 -c "from state_teach.mcp import mcp; from state_core.auth import load_credentials; from state_core.events import SqliteEventStore"`
2. Module import succeeds: `python3 -c "import state_teach.mcp"`
3. Import lint passes: `python3 -m state_core.import_lint` (exit 0)
</verification>

<done crit="state_teach/mcp.py imports state_core.auth.load_credentials and state_core.events.SqliteEventStore; imports resolve and exist at runtime; import lint passes"/>
</task>

<task number="2" type="auto" done="false">
<description>Add wiring test verifying state_teach can access state_core auth and events</description>
<implementation>

Create `tests/test_state_teach_wiring.py` with tests that verify:
1. `load_credentials` is importable from `state_core.auth` and callable
2. `SqliteEventStore` is importable from `state_core.events` and instantiable
3. The `mcp` FastMCP server still has its required tools after the wiring import (regression check)
</implementation>

<verification>
Run `python3 -m pytest tests/test_state_teach_wiring.py -v`
All tests pass.
</verification>

<done crit="New test file exists with wiring tests; all tests pass; no regression on existing teach module functionality"/>
</task>

</tasks>

<success_criteria>
- [ ] state_teach/mcp.py imports load_credentials from state_core.auth
- [ ] state_teach/mcp.py imports SqliteEventStore from state_core.events
- [ ] Import lint (state_core.import_lint) passes — no cross-mode violations
- [ ] Wiring test verifies the imports are functional
- [ ] Existing tests remain green
</success_criteria>

<verification>
- `python3 -m state_core.import_lint` exits 0
- `python3 -m pytest tests/test_state_teach_wiring.py tests/test_state_teach_mcp.py tests/test_state_teach_mcp_tools.py -v` passes
</verification>

<output>
- `src/state_teach/mcp.py` — modified (add imports)
- `tests/test_state_teach_wiring.py` — new (wiring tests)
</output>

<threat_model>
No auth or event store surfaces exposed in this plan — wiring only.
</threat_model>
