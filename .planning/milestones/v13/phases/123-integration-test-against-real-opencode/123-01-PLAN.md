---
phase: 123
phase_name: integration-test-against-real-opencode
plan: 123-01
type: verification
plan_name: MCP Server Integration Test Suite (state-teach)
autonomous: true
wave: 1
depends_on: ["115", "116", "117", "118", "119", "120", "121", "122"]
requirements: ["MCP-T-01", "MCP-T-02", "MCP-T-03", "MCP-T-04", "MCP-T-05", "MCP-T-06"]
files_created:
  - tests/test_state_teach_integration.py
gates: 0
---

## Phase 123 Plan: Integration Test Against Real Opencode MCP Client

**Goal:** E2E integration test proving the state-teach MCP server contract: registration, tool invocation, mode-gate, import lint, and structural integrity. Mirrors the Phase 114 pattern for state-build.

**Context:** All 8 prerequisite phases (115-122) are complete. The state-teach MCP server registers 14 skeleton tools behind a mode-gate. This final phase validates the entire server works end-to-end via direct Python import tests (subprocess E2E deferred per Phase 114 precedent).

---

### Tasks

#### 01. Create server registration test class
**Type:** auto
**Acceptance:** 3 tests: server name, tool count (14), tool descriptions ≤80 tokens

<action>
Create `tests/test_state_teach_integration.py` with `TestServerRegistration` class:

**test_server_name**: `mcp.name == "state-teach"`

**test_tool_count**: `len(mcp._tool_manager._tools) == 14` (MCP-T-03 has 14 tools)

**test_tool_descriptions_within_budget**: Every tool has a non-empty `description` attribute, and every description is ≤80 tokens (tiktoken o200k_base encoding). Validates MCP-T-02 compliance.

Uses `pytest.fixture(autouse=True)` to `os.chdir()` to project root.
</action>

<read_first>
- src/state_teach/mcp.py
- .planning/milestones/v13/REQUIREMENTS.md (MCP-T-02, MCP-T-03)
</read_first>

<acceptance_criteria>
- `pytest tests/test_state_teach_integration.py::TestServerRegistration -v` exits 0 with 3 passed
- Server name = "state-teach"
- Tool count = 14
- All descriptions present and ≤80 tokens
</acceptance_criteria>

#### 02. Create tool invocation test class
**Type:** auto
**Acceptance:** 3 tests: all tools invocable, SkeletonResponse validation, question_binding integration

<action>
Add `TestToolInvocation` class to the integration test file:

**test_all_14_tools_invocable**: Iterate over all 14 tools in `mcp._tool_manager._tools`. For each, get the tool function via `getattr(mcp_mod, name)` and call with no args. Assert response is a `SkeletonResponse` with `tool` matching name and `status == "not_implemented"`.

**test_skeleton_response_structure**: Explicitly validate `SkeletonResponse.model_json_schema()` has required fields `tool` and `status`.

**test_question_binding_integrated**: Import `ask_structured` from `state_teach.question_binding`, call with sample questions, verify the wire path from MCP module → question_binding works without import errors.

Uses `import state_teach.mcp as mcp_mod` to avoid shadowing.
</action>

<read_first>
- src/state_teach/mcp.py (all 14 tool functions, SkeletonResponse)
- src/state_teach/question_binding.py
</read_first>

<acceptance_criteria>
- `pytest tests/test_state_teach_integration.py::TestToolInvocation -v` exits 0 with 3 passed
- All 14 tools callable without errors
- SkeletonResponse schema valid
- question_binding importable from integration context
</acceptance_criteria>

#### 03. Create mode-gate integration test class
**Type:** auto
**Acceptance:** 2 tests: gate blocks build, gate allows teach/both

<action>
Add `TestModeGateIntegration` class:

**test_mode_gate_blocks_build**: Create `.state/mode.json` with `{"mode": "build"}` in a tmp_path. Assert `_check_mode_gate()` raises `SystemExit(78)`.

**test_mode_gate_allows_teach**: Create `.state/mode.json` with `{"mode": "teach"}` in a tmp_path. Assert `_check_mode_gate()` does NOT raise.

Complements Phase 115 unit tests by validating the integration path: the gate function called from the server entry point works correctly against real JSON files rather than mocks.
</action>

<read_first>
- src/state_teach/mcp.py (_check_mode_gate)
</read_first>

<acceptance_criteria>
- `pytest tests/test_state_teach_integration.py::TestModeGateIntegration -v` exits 0 with 2 passed
- build mode → SystemExit(78)
- teach mode → no exception
</acceptance_criteria>

#### 04. Create import-lint and structural integrity test class
**Type:** auto
**Acceptance:** 2 tests: cross-mode lint clean, module structure valid

<action>
Add `TestStructuralIntegrity` class:

**test_import_lint_clean**: Call `state_core.import_lint.lint()`, assert `exit_code == 0` with no cross-mode violations.

**test_module_structure_valid**: Verify `state_teach` package does NOT import from `state_build`. Verify all expected submodules are importable: `mcp`, `question_binding`, `observations`, `tokens`, `drill`, `kernel`, `concepts`, `mental_model`.
</action>

<read_first>
- src/state_core/import_lint.py
- src/state_teach/__init__.py
</read_first>

<acceptance_criteria>
- `pytest tests/test_state_teach_integration.py::TestStructuralIntegrity -v` exits 0 with 2 passed
- No cross-mode import violations
- All expected submodules importable
</acceptance_criteria>

### Verification

| Criterion | Check |
|-----------|-------|
| All 10 tests pass in < 1 second | `pytest tests/test_state_teach_integration.py -v` |
| Server contract: name = "state-teach" | TestServerRegistration |
| All 14 tools invocable | TestToolInvocation |
| Mode-gate correctly blocks build | TestModeGateIntegration |
| No cross-mode import violations | TestStructuralIntegrity |
| ruff clean on test file | `ruff check tests/test_state_teach_integration.py` |

### Integration Notes
- Follows Phase 114 pattern: direct Python imports, no subprocess spawning
- All tests must pass in < 1 second
- File must be importable from project root without conftest dependency
- Subprocess-based E2E (spawning `python -m state_teach.mcp` over stdio) deferred — opencode binary test infrastructure needed
