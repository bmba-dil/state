---
phase: 065
wave: 1
depends_on: [063]
files_modified:
  - src/state_worker/hooks.py (NEW)
  - src/state_worker/main.py
  - tests/test_worker_hooks.py (NEW)
autonomous: true
---

# Plan 065-01: Session Tear-Down

**Goal:** Worker buffers hook events during operation and flushes them to daemon on close signal before clean exit.
**Requirements:** WRK-10

### Tasks

#### 065.1 — Hook queue module
**Acceptance:** `state_worker.hooks` exports HookQueue with enqueue/flush_pending/flush_count
**Estimated effort:** Small
**Dependencies:** 063 (forward_hook)

#### 065.2 — Wire into main shutdown
**Acceptance:** main() creates HookQueue, flushes pending hooks before bridge disconnect on shutdown signal
**Estimated effort:** Small
**Dependencies:** 065.1

#### 065.3 — Tests
**Acceptance:** Tests cover enqueue, flush with success, flush with failures, empty flush
**Estimated effort:** Small

### must_haves
- HookQueue buffers hook events during session
- flush_pending forwards all buffered hooks to daemon
- flush_pending runs before bridge disconnect in shutdown
- Worker exits cleanly even if flush fails (best-effort flush)
