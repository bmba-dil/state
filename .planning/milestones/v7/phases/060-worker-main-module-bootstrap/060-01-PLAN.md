---
phase: 060
wave: 1
depends_on: []
files_modified:
  - src/state_worker/__init__.py
  - src/state_worker/main.py
  - src/state_worker/session.py (NEW)
  - tests/test_worker_main.py (NEW)
autonomous: true
---

# Plan 060-01: Worker Bootstrap + Daemon Attach

**Goal:** Implement the worker main module that reads a session ID, discovers the daemon socket, health-checks the daemon, and registers the session.

**Requirements:** WRK-10

### Tasks

#### 060.1 — Worker session identity module
**Acceptance:** `state_worker.session` module exists with `SessionIdentity` pydantic model and `resolve_session_id()` function
**Estimated effort:** Small
**Dependencies:** None
**Details:**
- Create `src/state_worker/session.py`
- `SessionIdentity` model: `session_id: str`, `project_root: str`, `pid: int`
- `resolve_session_id()`: reads `STATE_SESSION_ID` env var → fallback to CLI arg → fallback to `STATE-{pid}` auto-generated
- Follow `state_core/schema.py` pydantic patterns
- Use `Path.cwd()` for project_root default

<read_first>
- src/state_core/schema.py (for pydantic model conventions)
- src/state_worker/__init__.py
</read_first>

<action>
Create `src/state_worker/session.py`:
- `SessionIdentity(BaseModel)` with `session_id: str`, `project_root: str`, `pid: int`
- `resolve_session_id(session_id: str | None = None) -> SessionIdentity`
- Read `STATE_SESSION_ID` env var, fall back to arg, fall back to auto-generated `STATE-{os.getpid()}`
- Use `os.getpid()`, `os.environ.get()`, `Path.cwd().resolve()`
- No imports from state_build or state_teach (mode isolation rule from CLAUDE.md)
</action>

<acceptance_criteria>
- `grep -q "class SessionIdentity" src/state_worker/session.py`
- `grep -q "resolve_session_id" src/state_worker/session.py`
- `python3 -c "from state_worker.session import SessionIdentity, resolve_session_id; s = resolve_session_id(); assert s.session_id.startswith('STATE-'); assert s.pid > 0"` passes
</acceptance_criteria>

#### 060.2 — Worker daemon attach + health check
**Acceptance:** Worker can discover daemon socket, health-check it, and exit gracefully if daemon is down
**Estimated effort:** Medium
**Dependencies:** 060.1
**Details:**
- Extend `src/state_worker/main.py` with `attach_to_daemon()` function
- Read socket path from `.state/daemon.sock` via `state_daemon.socket.read_socket_path()`
- Health check: GET /health on Unix socket HTTP/1.1
- Return True if daemon responds `{"status": "ok"}`, False otherwise
- Handle socket not found, connection refused gracefully

<read_first>
- src/state_daemon/socket.py (read_socket_path function)
- src/state_daemon/server.py (_http_response, DaemonServer)
- src/state_worker/main.py
</read_first>

<action>
Implement `attach_to_daemon(session: SessionIdentity) -> bool` in `src/state_worker/main.py`:
- Call `read_socket_path()` to get daemon socket path
- If None, return False (daemon not running)
- Open Unix socket connection via `asyncio.open_unix_connection()`
- Send `GET /health HTTP/1.1\r\nHost: localhost\r\n\r\n`
- Read response, parse status line
- Return True if `200 OK` and body contains `{"status": "ok"}`
- Return False on connection errors, timeouts (2s), unexpected responses
- Use `structlog` for logging, import from `state_core.observability`
</action>

<acceptance_criteria>
- `grep -q "attach_to_daemon" src/state_worker/main.py`
- `grep -q "read_socket_path" src/state_worker/main.py`
- `grep -q "open_unix_connection" src/state_worker/main.py`
- `grep -q "GET /health" src/state_worker/main.py`
</acceptance_criteria>

#### 060.3 — Worker main entry point + signal handling
**Acceptance:** `python -m state_worker` entry point runs bootstrap, attaches to daemon, and exits gracefully on SIGTERM/SIGINT
**Estimated effort:** Medium
**Dependencies:** 060.2
**Details:**
- Implement `main()` async function in `src/state_worker/main.py`
- Parse optional `--session-id` CLI arg
- Call `resolve_session_id()`, `attach_to_daemon()`
- Register SIGTERM/SIGINT handlers for graceful shutdown
- Write PID file to `.state/worker-{session_id}.pid`
- Log startup/shutdown events via structlog
- Implement `__main__.py` for `python -m state_worker`

<read_first>
- src/state_daemon/orchestrator.py (for startup sequence pattern)
- src/state_daemon/pid.py (for PID file management pattern)
- src/state_worker/main.py
- src/state_worker/__init__.py
</read_first>

<action>
In `src/state_worker/main.py`:
- Add `main()` async function:
  1. Parse `--session-id` from `sys.argv`
  2. Resolve session identity via `resolve_session_id()`
  3. Write PID file to `.state/worker-{session.session_id}.pid` (atomic write pattern from daemon pid.py)
  4. Register SIGTERM/SIGINT handlers that set `shutdown_event.set()`
  5. Call `attach_to_daemon()` - log success/failure
  6. Await `shutdown_event.wait()`
  7. Clean up PID file, log shutdown
- Add `if __name__ == '__main__': asyncio.run(main())`
- Create `src/state_worker/__main__.py` that imports and runs `main()`
- Update `src/state_worker/__init__.py` to export `SessionIdentity`, `resolve_session_id`, `attach_to_daemon`, `main`
</action>

<acceptance_criteria>
- `grep -q "async def main" src/state_worker/main.py`
- `grep -q "signal.SIGTERM" src/state_worker/main.py`
- `grep -q "shutdown_event" src/state_worker/main.py`
- `ls src/state_worker/__main__.py` exists
- `python3 -c "import state_worker; from state_worker.main import main; assert callable(main)"` passes
</acceptance_criteria>

#### 060.4 — Worker main module tests
**Acceptance:** Tests cover session resolution, daemon attach (mock Unix socket), signal handling, PID file lifecycle
**Estimated effort:** Medium
**Dependencies:** 060.3
**Details:**
- Create `tests/test_worker_main.py`
- Test `resolve_session_id()` with env var, arg, auto-generate
- Test `attach_to_daemon()` with mocked Unix socket server
- Test `main()` with async fixtures
- Test PID file write/cleanup
- Use `pytest`, `pytest-asyncio`, `pytest-mock`
- Follow existing test conventions from `tests/` directory

<read_first>
- tests/ (any existing test files for patterns)
- src/state_worker/main.py
- src/state_worker/session.py
</read_first>

<action>
Create `tests/test_worker_main.py`:
- `TestSessionResolution`: test env var override, test CLI arg, test auto-generate fallback, test PID is positive
- `TestDaemonAttach`: mock `read_socket_path`, mock `asyncio.open_unix_connection`, test 200 OK, test connection refused, test timeout, test daemon not running (socket path None)
- `TestMainStartup`: test happy path (daemon up), test daemon down (graceful exit), test SIGTERM cleanup
- `TestPidFile`: test write on startup, test cleanup on shutdown
- Use `@pytest.mark.asyncio` for async tests
- Use `unittest.mock.patch` or `pytest-mock` mocker fixture
</action>

<acceptance_criteria>
- `ls tests/test_worker_main.py` exists
- Test file has at least 4 test classes
- `grep -q "pytest.mark.asyncio" tests/test_worker_main.py`
- `grep -q "resolve_session_id" tests/test_worker_main.py`
- `grep -q "attach_to_daemon" tests/test_worker_main.py`
</acceptance_criteria>

### Integration Notes
- Worker is phase 1 of 8 in v7 milestone; subsequent phases build on this foundation
- Phase 061 (daemon bridge) will extend `attach_to_daemon()` into a full HTTP+SSE client
- PID file format matches daemon pattern via atomic tempfile + rename
- Session identity flows through all phases: main → bridge → hooks → teardown

### Deviation Notes
- None — follows CONTEXT.md decisions exactly

### must_haves
- Worker main module imports without errors
- Session ID resolution works with env var, arg, and auto-generate fallbacks
- Worker discovers daemon socket and performs health check
- Worker PID file written atomically and cleaned up on exit
- SIGTERM/SIGINT trigger graceful shutdown sequence
