---
phase: 400-tier-definitions-state-machines
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/milestones/v40/phases/400/specs/TIER-ARC.md
  - .planning/milestones/v40/phases/400/specs/TIER-STAGE.md
  - .planning/milestones/v40/phases/400/specs/TIER-SLICE.md
  - .planning/milestones/v40/phases/400/specs/TIER-STEP.md
autonomous: true
requirements:
  - TIER-01
  - TIER-02
  - TIER-03
  - TIER-04
  - TIER-05

must_haves:
  truths:
    - "Every tier (Arc, Stage, Slice, Step) has a standalone specification document that defines its role, owned artifacts, state machine, behavioral primitives, and cross-tier relationships — readable without referencing other documents"
    - "All four tier spec documents consistently use the terms 'Stage' (not 'Phase'), 'design' and 'run' (not 'discuss' and 'execute'), and dash-prefix IDs like 'arc-01', 'stage-03', 'slice-12', 'step-5'"
    - "The Slice spec documents Steps as flat markdown FILES (step1PLAN.md) within the Slice folder, not subdirectories"
    - "Every tier spec includes a frontmatter fields table classifying each field as agent-owned or projector-owned"
  artifacts:
    - path: ".planning/milestones/v40/phases/400/specs/TIER-ARC.md"
      provides: "Arc tier specification"
      min_lines: 80
      contains: "State Machine</h2>"
      contains: "Owned Artifacts</h2>"
      contains: "Frontmatter Fields</h2>"
      contains: "arc-"
    - path: ".planning/milestones/v40/phases/400/specs/TIER-STAGE.md"
      provides: "Stage tier specification"
      min_lines: 80
      contains: "State Machine</h2>"
      contains: "Owned Artifacts</h2>"
      contains: "Frontmatter Fields</h2>"
      contains: "stage-"
      not_contains: "Phase"
    - path: ".planning/milestones/v40/phases/400/specs/TIER-SLICE.md"
      provides: "Slice tier specification"
      min_lines: 80
      contains: "stepNPLAN.md"
      contains: "DESIGN.md"
      contains: "Terminal Container"
      not_contains: "step-"  # as directory name
    - path: ".planning/milestones/v40/phases/400/specs/TIER-STEP.md"
      provides: "Step tier specification"
      min_lines: 80
      contains: "designing"
      contains: "running"
      contains: "not scheduler-dispatched"
      not_contains: "discussing"
      not_contains: "executing"
  key_links:
    - from: "TIER-ARC.md → TIER-STAGE.md"
      via: "Parent-of: Stages reference"
      pattern: "Stage"
    - from: "TIER-STAGE.md → TIER-SLICE.md"
      via: "Child-is: Slices reference"
      pattern: "Slice"
    - from: "TIER-SLICE.md → TIER-STEP.md"
      via: "Step-as-File reference"
      pattern: "stepNPLAN\\.md"
---

<objective>
Create four standalone tier specification documents (Arc, Stage, Slice, Step) that define
each tier's role in the hierarchy, state machine, owned artifacts, behavioral primitives,
and frontmatter fields. Each document must be readable independently without cross-referencing
other tier specs.

Purpose: These tier specs are the foundation for Phase 401's artifact catalog and all
downstream v41+ implementation (build kernel, agent harness, scheduler integration).
They are consumed by Plan 400-02 (state tables, events, schemas) and Plan 400-03
(semantics).

Output: Four markdown specification documents under `specs/` that together satisfy
TIER-01 through TIER-05 with D-01 through D-12 and D-17 through D-19 decisions honored.
</objective>

<execution_context>
$GSD_PATH/get-shit-done/workflows/execute-plan.md
$GSD_PATH/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v40/phases/400/400-CONTEXT.md
@.planning/milestones/v40/phases/400/400-RESEARCH.md
@.planning/milestones/v40/phases/400/400-PATTERNS.md
@.planning/milestones/v40/REQUIREMENTS.md
@.planning/milestones/v40/ROADMAP.md

<interfaces>
<!-- Key codebase patterns that the executor needs to follow. Extracted from PATTERNS.md. -->

From 400-PATTERNS.md — Tier specification document structure (follow for ALL four documents):
```markdown
## Role in Hierarchy
[One-paragraph definition of the tier's role in the four-tier system]

## State Machine
[ASCII/Mermaid diagram showing all states and transitions]

## State Transition Table
[Table: From | To | Event Trigger | Guard Condition | Budget Check]

## Owned Artifacts
| Artifact | Purpose | Schema Owner |
|----------|---------|-------------|
| ...      | ...     | Agent or Projector |

## Frontmatter Fields
| Field | Type | Owner | Required | Description |
|-------|------|-------|----------|-------------|

## Events
[Event type table with trigger conditions and state transitions]

## Cross-Tier Relationships
- Parent of: ...
- Sibling of: ...
- Child of: ...
```

From D-01 (LOCKED): "Phase" renamed to "Stage" — ALL documents use "Stage" for the second tier.
From D-03 (LOCKED): "discuss"→"design", "execute"→"run" — ALL Step states use "designing" and "running".
From D-17 (LOCKED): Exact states per tier:
  - Arc: planned → in_progress → auditing → shipped | abandoned  (≤4 forward states)
  - Stage: planned → in_progress → verified → shipped | abandoned  (≤4 forward states)
  - Slice: planned → worktree_ready → in_progress → shipped | reverted | blocked  (≤4 forward)
  - Step: idle → designing → planning → running → verifying → done | blocked | abandoned  (≤8 total)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Arc and Stage tier specification documents</name>
  <files>
    - .planning/milestones/v40/phases/400/specs/TIER-ARC.md
    - .planning/milestones/v40/phases/400/specs/TIER-STAGE.md
  </files>
  <read_first>
    - .planning/milestones/v40/phases/400/400-CONTEXT.md (DECISIONS D-01, D-02, D-05, D-07, D-08, D-09, D-10, D-17, D-18, D-19)
    - .planning/milestones/v40/phases/400/400-PATTERNS.md (lines 23-101: Arc pattern, Stage pattern)
    - .planning/milestones/v40/phases/400/400-RESEARCH.md (lines 330-398: specification patterns, transition table format, pydantic schema patterns)
    - .planning/milestones/v40/REQUIREMENTS.md (TIER-01, TIER-02, TIER-03)
  </read_first>
  <action>
Create two standalone specification documents following the per-tier document template
from 400-PATTERNS.md (lines 57-89 for Arc, lines 97-128 for Stage).

**TIER-ARC.md** — Arc tier specification (satisfies TIER-01, TIER-02):
1. ROLE: Define Arc as the coarsest scoping container — a feature block or project
   branch containing multiple Stages. Examples: "Auth System", "Plugin Framework".
2. STATE MACHINE (per D-17): 4 forward states (planned, in_progress, auditing, shipped)
   + 1 exit state (abandoned). Include ASCII diagram showing all transitions.
3. STATE TRANSITION TABLE with columns: From | To | Event Trigger | Guard Condition | Budget.
   - planned→in_progress: trigger `state.arc.started`, guard "≥1 child Stage is planned" (D-08).
   - in_progress→auditing: trigger `state.arc.stages_shipped`, guard "ALL child Stages are shipped" (D-18).
   - auditing→shipped: trigger `/state-ship-arc` manual command, guard "audit passes + ARC-SUMMARY.md produced" (D-18).
   - *→abandoned: trigger `state.arc.abandoned`, guard "explicit abandon command".
4. OWNED ARTIFACTS (per D-05): CRIT.md (agent), MAP.md (agent), ARC.md (agent), STATE.md (projector).
5. FRONTMATTER FIELDS table. Agent-owned: id (arc-{n} per D-01), title, goal, success_criteria,
   depends_on. Projector-owned: status, stage_count, shipped_stage_count, completed_at.
   All fields classified exactly once per TIER-07.
6. EVENTS table: state.arc.created, state.arc.started, state.arc.stages_shipped,
   state.arc.shipped, state.arc.abandoned, state.arc.updated.
7. CROSS-TIER: Parent of Stages. Sibling of other Arcs (depends_on only within same Arc
   per D-10). Child of none (root tier). ID format: arc-{n} (D-01). Slugs: arc-{n}-{slug} (D-02).

**TIER-STAGE.md** — Stage tier specification (satisfies TIER-01, TIER-03):
1. ROLE: Define Stage as the decomposition layer under Arc, containing Slices. A Stage
   groups related Slices and tracks cross-Slice verification. Renamed from "Phase" (D-01).
   Include a MIGRATION NOTE box at the top: "This tier was formerly called 'Phase' in
   GSD lineage. The rename to 'Stage' avoids confusion with workflow phases (design phase,
   run phase, verify phase). Existing codebase event types (state.phase.*) will be migrated
   to state.stage.* in v41+."
2. STATE MACHINE (per D-17): 4 forward states (planned, in_progress, verified, shipped)
   + 1 exit state (abandoned). Include ASCII diagram.
3. STATE TRANSITION TABLE:
   - planned→in_progress: trigger `state.stage.started`, guard "≥1 child Slice is worktree_ready".
   - in_progress→verified: trigger `state.stage.slices_shipped`, guard "ALL child Slices are shipped"
     (per D-19 composite state — projector checks child states).
   - verified→shipped: trigger `state.stage.completed`, guard "manual verify-stage command passes".
   - *→abandoned: trigger `state.stage.abandoned`, guard "explicit abandon; cascade dependents→blocked (D-13)".
4. OWNED ARTIFACTS (per D-05): CRIT.md (agent), MAP.md (agent), STAGE.md (agent), STATE.md (projector).
5. FRONTMATTER FIELDS: Agent-owned: id (stage-{n} per D-01), title, goal, success_criteria,
   arc_id (parent), depends_on (D-12 format). Projector-owned: status, slice_count,
   shipped_slice_count, completed_at. Field ownership classified exactly once (TIER-07).
6. EVENTS: state.stage.created, state.stage.started, state.stage.slices_shipped,
   state.stage.completed, state.stage.abandoned, state.stage.updated. Note migration
   from state.phase.* to state.stage.*.
7. CROSS-TIER: Parent of Slices. Child of ONE Arc (parent). Sibling of Stages within same
   Arc (depends_on same-parent-only per D-10). ID: stage-{n}. Slugs: stage-{n}-{slug}.

**Consumed by:** Plan 400-02 (FSM-TABLES.md references these state definitions; EVENT-TAXONOMY.md
references these event lists; FRONTMATTER-SCHEMAS.md references these frontmatter field tables).
Plan 400-03 (DESC-SEMANTICS.md references these state machines for abandon/blocked/decimal rules).
Phase 401 (ART-01 through ART-05, DSK-01 through DSK-06) reads these to build the artifact catalog.
  </action>
  <verify>
    <automated>grep -c 'State Machine' .planning/milestones/v40/phases/400/specs/TIER-ARC.md && grep -c 'State Machine' .planning/milestones/v40/phases/400/specs/TIER-STAGE.md</automated>
  </verify>
  <acceptance_criteria>
    - TIER-ARC.md ≥80 lines, contains all 7 sections (Role, State Machine, Transition Table, Artifacts, Frontmatter, Events, Cross-Tier)
    - TIER-STAGE.md ≥80 lines, contains "Migration Note" or "formerly called Phase", uses "Stage" not "Phase" throughout
    - Both use dash-prefix IDs (arc-{n}, stage-{n}) with no leading zeros per D-01
    - TIER-STAGE.md frontmatter table includes arc_id as agent-owned parent reference field
    - Both include ASCII transition diagrams showing all states from D-17
    - Both have transition tables with Event Trigger, Guard Condition, and Budget columns
    - grep -c 'Phase' TIER-STAGE.md returns 0 (except Migration Note which explicitly says "formerly called Phase")
  </acceptance_criteria>
  <done>Arc and Stage tier spec documents exist with all required sections, D-01/D-02/D-05/D-08/D-17/D-18/D-19 decisions honored. Both documents readable independently.</done>
</task>

<task type="auto">
  <name>Task 2: Create Slice and Step tier specification documents</name>
  <files>
    - .planning/milestones/v40/phases/400/specs/TIER-SLICE.md
    - .planning/milestones/v40/phases/400/specs/TIER-STEP.md
  </files>
  <read_first>
    - .planning/milestones/v40/phases/400/400-CONTEXT.md (DECISIONS D-03, D-04, D-06, D-11, D-17, D-19)
    - .planning/milestones/v40/phases/400/400-PATTERNS.md (lines 131-258: Slice pattern, Step pattern)
    - .planning/milestones/v40/phases/400/400-RESEARCH.md (lines 171-181: Slice FSM diagram with 6 states; lines 183-201: Step FSM diagram with renamed states)
    - .planning/milestones/v40/REQUIREMENTS.md (TIER-01, TIER-04, TIER-05)
  </read_first>
  <action>
Create two standalone specification documents. The Slice is the terminal container;
Steps are flat FILES within it (not subdirectories).

**TIER-SLICE.md** — Slice tier specification (satisfies TIER-01, TIER-04):
1. ROLE: Define Slice as the terminal container tier — the work unit that gets its own
   worktree. Contains Steps (as flat files) and all workflow artifacts. One Slice maps
   to one coherent unit of work (e.g., "add JWT auth", "migrate to litellm 2.0").
2. CRITICAL (D-04): State explicitly: "Steps are markdown FILES (`step1PLAN.md`,
   `step2PLAN.md`...) within the Slice folder — NOT subdirectories. The Slice folder
   is the terminal container; no further directory nesting for Steps."
3. STATE MACHINE (per D-17): 4 forward states (planned, worktree_ready, in_progress,
   shipped) + 3 alternative states (reverted, blocked, deferred). Include ASCII diagram
   showing ALL states and transitions including:
   - reverted→planned (explicit revert, worktree destroy + rebuild)
   - blocked→in_progress (dep resolved, unblocked)
   - *→blocked (abandon-cascade per D-13)
4. STATE TRANSITION TABLE:
   - planned→worktree_ready: trigger `state.slice.worktree_ready`, guard "worktree bootstrap completes".
   - worktree_ready→in_progress: trigger `state.slice.started`, guard "≥1 child Step is idle".
   - in_progress→shipped: trigger `state.slice.shipped` (composite from Steps per D-19),
     guard "ALL child Steps are done + VERIFICATION.md passes".
   - *→blocked: trigger `state.slice.blocked`, guard "contains blocked_reason in frontmatter".
     Blocked entered via abandon-cascade (D-13) when a dependency Slice is abandoned.
   - *→reverted: trigger `state.slice.reverted`, guard "explicit revert command".
   - planned→deferred: trigger `state.slice.deferred`, guard "deferred_reason set in frontmatter" (D-14).
   - deferred→planned: trigger `state.slice.undeferred`, guard "manual un-defer".
5. OWNED ARTIFACTS (per D-04, D-05, D-06): Slice-level files:
   - SLICE.md (agent-authored definition + frontmatter)
   - DESIGN.md (design phase output, per D-03/D-06)
   - RESEARCH.md (research phase output, per D-06)
   - step1PLAN.md, step2PLAN.md, ... stepNPLAN.md (run-phase tracking files, per D-04/D-06)
   - VERIFICATION.md (verify phase output)
   - SUMMARY.md (summary phase output)
   - STATE.md (projector-rebuilt state projection)
   - Additional optional sub-flow artifacts: REVIEW.md, LOCK-IN.md, SHIP.md (per D-05)
   Workflow order (D-06): design → research → run (generates step files) → verify → summary
   → optional sub-flows.
6. FRONTMATTER FIELDS: Agent-owned: id (slice-{n}), title, goal, success_criteria,
   stage_id (parent), depends_on (D-12 format with edge types: blocks/soft/data).
   Projector-owned: status, step_count, completed_step_count, worktree_dir,
   worktree_branch, completed_at, deferred_reason. All fields classified exactly once (TIER-07).
7. EVENTS: state.slice.created, state.slice.worktree_ready, state.slice.started,
   state.slice.shipped, state.slice.reverted, state.slice.blocked, state.slice.deferred,
   state.slice.undeferred, state.slice.updated.
8. CROSS-TIER: Parent of Steps. Child of ONE Stage. Depends on other Slices within same
   Stage only (D-10). ID: slice-{n} (D-01). Slugs: slice-{n}-{slug} (D-02).
   Worktree: one per Slice, created at worktree_ready transition. Decimal insertions
   at Slice level: slice-12.1, slice-12.2 (D-16).

**TIER-STEP.md** — Step tier specification (satisfies TIER-01, TIER-05):
1. ROLE: Define Step as the smallest unit of work — a single discuss→plan→execute→verify
   cycle. Steps are NOT scheduler-dispatched (D-11); they run SERIALLY within a single
   agent session. The agent is responsible for: organizing work, delegating to subagents,
   synthesizing work, moving to next steps, finalizing work, and initiating verification.
2. STATE MACHINE (per D-17 with D-03 renames): 6 forward states (idle, designing,
   planning, running, verifying, done) + 2 exit states (blocked, abandoned).
   Include ASCII diagram. CRITICAL: Use "designing" NOT "discussing", "running" NOT
   "executing" (D-03 LOCKED rename).
3. STATE TRANSITION TABLE:
   - idle→designing: trigger `state.step.designed`, guard "Step created, agent invokes design".
   - designing→planning: trigger `state.step.planned`, guard "DESIGN.md completed".
     Idle→planning direct: trigger `state.step.planned`, guard "design skipped by agent
     (simple work, no research needed)".
   - planning→running: trigger `state.step.ran`, guard "PLAN.md completed".
   - running→verifying: trigger `state.step.verify_started`, guard "all sub-steps complete".
   - verifying→done: trigger `state.step.verify_passed`, guard "VERIFICATION.md passes".
   - verifying→running: trigger `state.step.verify_failed`, guard "fix needed; loop back to running".
   - *→blocked: trigger `state.step.blocked`, guard "blocked_reason set".
   - *→abandoned: trigger `state.step.abandoned`, guard "explicit abandon".
     Blocked→running: trigger `state.step.unblocked`, guard "blocking condition resolved" (D-15).
4. SUB-STEP NOTE (D-04): Steps CAN contain sub-steps (1.1, 1.2, 1.3) within a single
   file. Sub-step parallelism is an agent-internal decision — no scheduler involvement.
   This is a v41+ harness concern, documented here for design contract.
5. OWNED ARTIFACTS: Step tracking files (step1PLAN.md through stepNPLAN.md) live in
   the parent Slice folder per D-04. The Step itself has no dedicated directory.
   Step-level data is tracked via event store (SQLite authoritative) and projected
   into the Slice's STATE.md.
6. FRONTMATTER FIELDS (for step tracking files): Agent-owned: step_number, title, goal,
   plan_summary, verification_criteria. Projector-owned: status, completed_at.
   All fields classified exactly once (TIER-07). Status uses D-03 renamed values:
   idle, designing, planning, running, verifying, done, blocked, abandoned.
7. EVENTS: state.step.created, state.step.designed (was state.step.discussed),
   state.step.planned, state.step.ran (was state.step.executed), state.step.verify_started,
   state.step.verify_passed, state.step.verify_failed, state.step.advanced,
   state.step.blocked, state.step.unblocked, state.step.abandoned.
   Include migration note table mapping old event names to new (from 400-PATTERNS.md line 220-228).
8. CROSS-TIER: Child of ONE Slice. No siblings (Steps run serial, not parallel).
   Steps do NOT have their own `depends_on` — intra-Slice ordering is the agent's
   responsibility (D-11). ID: step-{n} (D-01). Steps do NOT get slugs (terminal unit).

**Consumed by:** Plan 400-02 (FSM-TABLES.md needs Slice/Step states; EVENT-TAXONOMY.md
needs Slice/Step events; COMPOSITE-CASCADE.md needs Slice→Step hierarchy).
Plan 400-03 (DESC-SEMANTICS.md needs Slice blocked/reverted/deferred states).
  </action>
  <verify>
    <automated>grep -c 'State Machine' .planning/milestones/v40/phases/400/specs/TIER-SLICE.md && grep -c 'State Machine' .planning/milestones/v40/phases/400/specs/TIER-STEP.md</automated>
  </verify>
  <acceptance_criteria>
    - TIER-SLICE.md ≥80 lines, contains all 8 sections, explicitly states "Steps are markdown FILES...NOT subdirectories"
    - TIER-SLICE.md lists stepNPLAN.md as a Slice-level artifact (flat file, not subdirectory)
    - TIER-STEP.md ≥80 lines, uses "designing" and "running" (not "discussing" or "executing") per D-03
    - TIER-STEP.md explicitly documents "Steps are NOT scheduler-dispatched (D-11)" and "Steps run SERIALLY within one agent session"
    - Both use dash-prefix IDs with no leading zeros per D-01
    - TIER-SLICE.md includes reverted, blocked, and deferred states with transition rules
    - TIER-SLICE.md workflow order matches D-06: design → research → run → verify → summary
    - TIER-STEP.md state names match D-17 with D-03 renames: idle, designing, planning, running, verifying, done, blocked, abandoned
    - grep -c 'executing' TIER-STEP.md returns 0
    - grep -c 'discussing' TIER-STEP.md returns 0
    - grep -c 'steps/</h3>' TIER-SLICE.md returns 0 (no step subdirectory in layout)
  </acceptance_criteria>
  <done>Slice and Step tier spec documents exist with all required sections. Slice documented as terminal container with Steps as flat files. Step FSM uses D-03 renamed states. All D-01/D-03/D-04/D-06/D-11 decisions honored.</done>
</task>

<task type="auto">
  <name>Task 3: Cross-tier consistency verification</name>
  <files>
    - .planning/milestones/v40/phases/400/specs/TIER-ARC.md
    - .planning/milestones/v40/phases/400/specs/TIER-STAGE.md
    - .planning/milestones/v40/phases/400/specs/TIER-SLICE.md
    - .planning/milestones/v40/phases/400/specs/TIER-STEP.md
  </files>
  <read_first>
    - .planning/milestones/v40/phases/400/specs/TIER-ARC.md
    - .planning/milestones/v40/phases/400/specs/TIER-STAGE.md
    - .planning/milestones/v40/phases/400/specs/TIER-SLICE.md
    - .planning/milestones/v40/phases/400/specs/TIER-STEP.md
    - .planning/milestones/v40/phases/400/400-CONTEXT.md (all D-XX decisions for cross-reference)
  </read_first>
  <action>
Perform a systematic cross-tier consistency audit of all four tier specification documents.
Fix any inconsistencies found. This task produces no new files — it edits existing tier specs.

Check each consistency dimension and fix discrepancies:

1. **Naming convention consistency (D-01, D-02, D-03):**
   - ALL documents use dash-prefix IDs with no leading zeros: arc-{n}, stage-{n}, slice-{n}, step-{n}
   - TIER-ARC.md, TIER-STAGE.md, TIER-SLICE.md include slug format documentation (D-02)
   - TIER-STEP.md uses "designing" and "running" (D-03). No "discussing" or "executing" anywhere.
   - grep all documents for "Phase" — only TIER-STAGE.md migration note should contain it.
     Remove any stray "Phase" references from other documents.

2. **Parent-child relation consistency:**
   - TIER-ARC.md "Cross-Tier" says "Parent of: Stages" → TIER-STAGE.md "Cross-Tier" says "Child of: ONE Arc" ✓
   - TIER-STAGE.md "Cross-Tier" says "Parent of: Slices" → TIER-SLICE.md "Cross-Tier" says "Child of: ONE Stage" ✓
   - TIER-SLICE.md "Cross-Tier" says "Parent of: Steps" → TIER-STEP.md "Cross-Tier" says "Child of: ONE Slice" ✓
   - If any mismatch, fix to align child→parent and parent→child declarations.

3. **State budget consistency (D-17 with FSM-06 resolution):**
   - Arc: 4 forward states (planned, in_progress, auditing, shipped) ≤4 ✓. Abandoned is exit state.
     Per RESEARCH.md Open Question Q1: interpret "auditing" as a discrete machine state
     (not projector-computed) per D-18 entry guard. This gives Arc exactly 4 forward states,
     satisfying both D-17 (≤4) and FSM-06's intent. Add a note in TIER-ARC.md: "Auditing
     is a discrete machine state with an entry guard (all child Stages shipped), not a
     projector-computed composite state. Composite states (D-19) are computed separately."
   - Stage: 4 forward states (planned, in_progress, verified, shipped) ≤4 ✓
   - Slice: 4 forward states (planned, worktree_ready, in_progress, shipped) ≤4 ✓.
     Blocked/reverted/deferred are alternative/exit states, not forward-progression.
   - Step: 6 forward states + 2 exit states = 8 total ≤8 ✓
   - Verify every transition table's Budget column shows ≤N check passing for the correct N.

4. **Depends-on format consistency (D-10, D-12):**
   - All frontmatter field tables that include `depends_on` use the D-12 format:
     `depends_on: [{id: "stage-02", edge: "blocks"}]` with edge types: blocks, soft, data.
   - Key name is `edge` (not `kind`), matching D-12 and CONTEXT.md examples.
   - Same-parent-only constraint (D-10) documented consistently: Stages depend on Stages
     within same Arc; Slices depend on Slices within same Stage.
   - Steps do NOT have depends_on (D-11).

5. **Event name consistency across tier specs:**
   - Events listed in each tier spec match the naming convention: state.{tier}.{action}
   - TIER-STAGE.md uses state.stage.* NOT state.phase.* (forward-looking)
   - TIER-STEP.md uses state.step.designed NOT state.step.discussed (D-03)
   - TIER-STEP.md uses state.step.ran NOT state.step.executed (D-03)
   - Include migration notes where old event names differ from new.

6. **Frontmatter classification consistency (TIER-07):**
   - Every field in every tier's frontmatter table is classified EXACTLY ONCE as
     "Agent" or "Projector". No field listed as both. No field unclassified.
   - Projector-owned fields across all tiers: status, *_count fields, completed_at
   - Agent-owned fields across all tiers: id, title, goal, success_criteria, parent_id,
     depends_on, deferred_reason (when present)
   - Verify no tier claims to own a field another tier also claims.

**Consumed by:** Plan 400-02 executor (reads all four tier specs to build FSM tables,
event taxonomy, and schemas). Plan 400-03 executor (reads all four tier specs for
descope/blocked/decimal semantics).
  </action>
  <verify>
    <automated>grep -rn 'Phase' .planning/milestones/v40/phases/400/specs/TIER-ARC.md .planning/milestones/v40/phases/400/specs/TIER-SLICE.md .planning/milestones/v40/phases/400/specs/TIER-STEP.md 2>/dev/null; grep -rn 'discussing\|executing' .planning/milestones/v40/phases/400/specs/TIER-STEP.md 2>/dev/null; grep -rn 'state\.phase\.' .planning/milestones/v40/phases/400/specs/TIER-STAGE.md 2>/dev/null; echo "CONSISTENCY CHECK: Verify no false positives above (Phase only in Stage migration note; old Step state names absent; old event names only in migration notes)"</automated>
  </verify>
  <acceptance_criteria>
    - [ ] All four tier spec files exist: specs/TIER-ARC.md, TIER-STAGE.md, TIER-SLICE.md, TIER-STEP.md
    - [ ] TIER-STAGE.md uses "Stage" not "Phase" throughout (grep -c 'Phase' returns 0 except in migration note)
    - [ ] TIER-STEP.md uses "design"/"run" not "discuss"/"execute" (D-03 verified via grep)
    - [ ] TIER-STEP.md specifies Step as markdown files, NOT folders (grep 'Step.*folder\|Step.*directory' returns 0)
    - [ ] TIER-SLICE.md specifies Slice as terminal container holding all files flat
    - [ ] Each tier spec has: role section, owned artifacts table, behavioral primitives, cross-tier relationships, frontmatter field table with agent/projector classification
    - [ ] Cross-tier consistency: no field owned by both agent and projector across any tier; event names match state.{tier}.{action} convention; old event names are only in migration notes
  </acceptance_criteria>
  <done>All four tier specification documents are self-contained, consistent with each other, and ready for Plan 400-02 to consume them for FSM/event/pydantic work.</done>
</task>
</tasks>