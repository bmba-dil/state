---
phase: 401-artifact-catalog-naming-layout-cross-refs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [.planning/milestones/v40/phases/401/specs/ARTIFACT-CATALOG.md, .planning/milestones/v40/phases/401/specs/SCHEMA-OWNERSHIP.md]
autonomous: true
requirements: [ART-01, ART-02, ART-03, ART-04, ART-05]
must_haves:
  truths:
    - "Every artifact file type across all four tiers is documented with purpose, owning tier, schema ownership, creation/update triggers, and file format"
    - "Templates exist for all 13 artifact types with frontmatter fields matching Phase 400 pydantic models"
    - "Immutability rules are specified per artifact — what locks, when it locks, what stays mutable"
    - "Every artifact is classified as agent-authored or projector-rebuilt with clear boundaries"
    - "Schema validation rules define when validation fires and what it checks"
  artifacts:
    - path: ".planning/milestones/v40/phases/401/specs/ARTIFACT-CATALOG.md"
      provides: "Complete catalog of 13 artifact types with structured entries, embedded templates, and immutability rules"
      min_lines: 400
    - path: ".planning/milestones/v40/phases/401/specs/SCHEMA-OWNERSHIP.md"
      provides: "Projection-vs-authored classification table, MAP.md hybrid ownership detail, schema validation rules"
      min_lines: 150
  key_links:
    - from: "specs/ARTIFACT-CATALOG.md each artifact entry"
      to: "Phase 400 specs/FRONTMATTER-SCHEMAS.md §ArcFrontmatter/StageFrontmatter/SliceFrontmatter/StepFrontmatter"
      via: "Frontmatter Model field in entry property table"
      pattern: "Frontmatter Model.*FRONTMATTER-SCHEMAS\\.md"
    - from: "specs/SCHEMA-OWNERSHIP.md"
      to: "Phase 400 specs/FRONTMATTER-SCHEMAS.md §Field Ownership Rules table"
      via: "Cross-reference to field ownership table"
      pattern: "TIER-07"
---

<objective>
Create the complete artifact catalog and schema ownership specification for the `.state/build/` filesystem blueprint.

Purpose: Define every artifact type, its template, immutability rules, projection-vs-authored classification, and schema validation rules — providing the design contract consumed by Plan 401-02 (directory layout), Plan 401-03 (cross-references), and v41+ runtime implementation.

Output: Two specification documents — `ARTIFACT-CATALOG.md` (ART-01 through ART-03: catalog + templates + immutability) and `SCHEMA-OWNERSHIP.md` (ART-04 through ART-05: projection classification + validation rules).
</objective>

<execution_context>
$GSD_PATH/get-shit-done/workflows/execute-plan.md
$GSD_PATH/get-shit-done/templates/summary.md
</execution_context>

<context>
<!-- Phase 400 prerequisite specs — these define the tier structure, state machines, and frontmatter models -->
@.planning/milestones/v40/phases/400/specs/TIER-ARC.md
@.planning/milestones/v40/phases/400/specs/TIER-STAGE.md
@.planning/milestones/v40/phases/400/specs/TIER-SLICE.md
@.planning/milestones/v40/phases/400/specs/TIER-STEP.md
@.planning/milestones/v40/phases/400/specs/FRONTMATTER-SCHEMAS.md
@.planning/milestones/v40/phases/400/specs/DESC-SEMANTICS.md
@.planning/milestones/v40/phases/400/400-CONTEXT.md

<!-- Phase 401 context — locked decisions and research patterns -->
@.planning/milestones/v40/phases/401/401-CONTEXT.md
@.planning/milestones/v40/phases/401/401-RESEARCH.md
@.planning/milestones/v40/phases/401/401-PATTERNS.md

<interfaces>
<!-- Key types and contracts from Phase 400 that ARTIFACT-CATALOG.md references -->

From Phase 400 FRONTMATTER-SCHEMAS.md — pydantic frontmatter models for all 4 tiers:
- `ArcFrontmatter` — 5 agent fields (id, title, goal, success_criteria, depends_on), 4 projector fields (status, stage_count, shipped_stage_count, completed_at). Status enum: planned, in_progress, auditing, shipped, abandoned. Extra="forbid".
- `StageFrontmatter` — 6 agent fields (id, title, goal, success_criteria, arc_id, depends_on), 4 projector fields (status, slice_count, shipped_slice_count, completed_at). Status enum: planned, in_progress, verified, auditing, shipped, abandoned.
- `SliceFrontmatter` — 6 agent fields (id, title, goal, success_criteria, stage_id, depends_on), 7 projector fields (status, step_count, completed_step_count, worktree_dir, worktree_branch, completed_at), 2 agent reason fields (deferred_reason, blocked_reason). Status enum: planned, worktree_ready, in_progress, shipped, reverted, blocked, deferred.
- `StepFrontmatter` — 6 agent fields (step_number, title, goal, slice_id, plan_summary, verification_criteria), 2 projector fields (status, completed_at), 1 agent reason field (blocked_reason). Status enum: idle, designing, planning, running, verifying, done, blocked, abandoned.

From Phase 400 D-01: ID format = dash-prefix, no leading zeros: `arc-{n}`, `stage-{n}`, `slice-{n}`, `step-{n}`.
From Phase 400 D-02: Kebab-case slugs optional in directory names (not part of stable ID).
From Phase 400 D-04: Steps are markdown FILES (step1PLAN.md, step2PLAN.md...), NOT folders.
From Phase 400 D-05: CRIT.md at Arc and Stage; MAP.md at Arc and Stage; Slice-level: DESIGN.md, RESEARCH.md, stepNPLAN.md, VERIFICATION.md, SUMMARY.md.
From Phase 400 D-12: Edge types: blocks (hard prerequisite), soft (advisory), data (artifact-producing + hard).
From Phase 400 D-17: Tier state machines with status values.

From 401-CONTEXT.md locked decisions:
- D-401-01: ARC.md and STAGE.md are agent-authored (projector updates status only)
- D-401-02: DECISIONS.md at each tier (agent-authored, append-only)
- D-401-03: CRIT.md = numbered falsifiable requirements (CRIT-01, CRIT-02...)
- D-401-04: MAP.md = generated plan + tracker with projector-updated checkboxes
- D-401-09: Agent-authored artifacts list (10 types)
- D-401-10: Projector-rebuilt artifacts (index.json, consolidated STATE.md)
- D-401-11: MAP.md checkboxes updated by projector on state.stage.*/state.slice.* events
- D-401-12: stepNPLAN.md and CRIT.md lock when Slice enters in_progress; DESIGN.md/RESEARCH.md remain mutable
- D-401-13: Snapshot-before-mutation protocol at daemon level — blocked, not warned
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ARTIFACT-CATALOG.md — complete artifact catalog with templates and immutability rules</name>
  <files>.planning/milestones/v40/phases/401/specs/ARTIFACT-CATALOG.md</files>

  <read_first>
  <!-- Files this task MUST read before creating any content -->
  - .planning/milestones/v40/phases/400/specs/TIER-ARC.md (lines 1-124) — Arc tier definition, owned artifacts, directory path
  - .planning/milestones/v40/phases/400/specs/TIER-STAGE.md (full) — Stage tier definition, owned artifacts
  - .planning/milestones/v40/phases/400/specs/TIER-SLICE.md (full) — Slice tier definition, owned artifacts
  - .planning/milestones/v40/phases/400/specs/TIER-STEP.md (full) — Step tier definition, owned artifacts
  - .planning/milestones/v40/phases/400/specs/FRONTMATTER-SCHEMAS.md (lines 16-195) — all four frontmatter models, field lists, status enums
  - .planning/milestones/v40/phases/400/specs/DESC-SEMANTICS.md (lines 1-50) — abandon cascade, deferment semantics
  - .planning/milestones/v40/phases/401/401-CONTEXT.md (lines 16-59) — D-401-01 through D-401-17 locked decisions
  - .planning/milestones/v40/phases/401/401-PATTERNS.md (lines 36-109) — catalog entry pattern, template structure pattern
  </read_first>

  <action>
  Create `.planning/milestones/v40/phases/401/specs/ARTIFACT-CATALOG.md` — a single comprehensive specification document covering ART-01 (catalog of all 13 artifact types), ART-02 (embedded templates for each), and ART-03 (immutability rules per artifact).

  **STRUCTURE: Follow the spec document format from PATTERNS.md lines 540-581:**
  - Header: `> **Design contract for v41+ runtime filesystem implementation.**` + `Consumed by: v41+ (runtime), v42 (quality pipeline), v43 (GSD command ports), Phase 401 Plan 02 (DIRECTORY-TREE.md), Phase 401 Plan 03 (CROSS-REFERENCES.md).`
  - Design Conventions section (1-2 paragraphs establishing scope, referencing locked decisions D-401-01 through D-401-13 by ID)
  - Artifact Catalog section — structured entries for ALL 13 artifact types
  - Templates section — inline YAML frontmatter templates for each type
  - Immutability Rules section — per-artifact lock rules table
  - Cross-References to Phase 400 section
  - Threat Model Alignment section

  **SECTION 1: Artifact Catalog (ART-01) — Document these 13 artifact types:**
  
  1. ARC.md (Arc tier — agent-authored, status by projector)
  2. STAGE.md (Stage tier — agent-authored, status by projector)
  3. SLICE.md (Slice tier — agent-authored)
  4. CRIT.md (Arc and Stage tiers — agent-authored)
  5. MAP.md (Arc and Stage tiers — hybrid: agent writes content, projector updates checkboxes per D-401-11)
  6. DECISIONS.md (Arc and Stage tiers — agent-authored, append-only per D-401-02)
  7. DESIGN.md (Slice tier — agent-authored)
  8. RESEARCH.md (Slice tier — agent-authored)
  9. stepNPLAN.md (Slice tier — agent-authored, one file per Step)
  10. VERIFICATION.md (Slice tier — agent-authored)
  11. SUMMARY.md (Slice tier — agent-authored)
  12. index.json (Projector-rebuilt at `.state/build/index.json` per D-401-10)
  13. STATE.md projections (Projector-rebuilt under `.state/build/state/` per D-401-07)

  **For EACH artifact entry, include this EXACT property table format** (from PATTERNS.md lines 91-109):

  | Property | Value |
  |----------|-------|
  | **Purpose** | [1-sentence: what this artifact represents, who creates it] |
  | **Owning Tier** | [Arc / Stage / Slice / N/A (projector-level)] |
  | **Schema Owner** | [Agent / Projector / Hybrid] — with breakdown of which fields belong to which owner |
  | **Created By** | [which CLI command or workflow step creates it] |
  | **Creation Trigger** | [what event or user action causes creation] |
  | **Updated By** | [Agent / Projector / Both] — with specific field-level detail |
  | **Update Triggers** | [specific event types from Phase 400 EVENT-TAXONOMY.md that trigger updates] |
  | **File Format** | Markdown with YAML frontmatter / JSON |
  | **Frontmatter Model** | [If agent-authored: ArcFrontmatter / StageFrontmatter / SliceFrontmatter / StepFrontmatter — reference Phase 400 FRONTMATTER-SCHEMAS.md §section. If projector: N/A — schema defined in this document.] |
  | **Immutability** | [When fields lock. Examples: "depends_on and success_criteria lock when Arc enters in_progress" (ARC.md), "all fields lock when Slice enters in_progress" (stepNPLAN.md per D-401-12), "always mutable" (DESIGN.md)] |
  | **Cross-Refs From** | [What artifact types reference this one, and in what field. Example: "sibling Arcs via depends_on; child Stages via arc_id"] |
  | **Cross-Refs To** | [What this artifact references. Example: ARC.md → "sibling Arcs via depends_on; child Stages listed in MAP.md"] |
  | **File Path** | [Exact relative path from `.state/build/` root. Use variable notation `arc-{n}`, `stage-{n}`, `slice-{n}`, `step-{n}` consistently.] |

  **CRITICAL: Use the EXACT SAME path variable notation everywhere.** The notation is:
  - `arc-{n}` (never `{arc-id}`, `arc_id`, `{arc}`)
  - `stage-{n}` (never `{stage-id}`, `stage_id`)
  - `slice-{n}` (never `{slice-id}`, `slice_id`)
  - `step-{n}` (never `{step-id}`, `step_id`)

  **File paths must be CONSISTENT across all entries.** The canonical paths are:

  | Artifact | Canonical Path |
  |----------|---------------|
  | ARC.md | `arcs/arc-{n}/ARC.md` |
  | CRIT.md (Arc) | `arcs/arc-{n}/CRIT.md` |
  | MAP.md (Arc) | `arcs/arc-{n}/MAP.md` |
  | DECISIONS.md (Arc) | `arcs/arc-{n}/DECISIONS.md` |
  | STAGE.md | `arcs/arc-{n}/stages/stage-{n}/STAGE.md` |
  | CRIT.md (Stage) | `arcs/arc-{n}/stages/stage-{n}/CRIT.md` |
  | MAP.md (Stage) | `arcs/arc-{n}/stages/stage-{n}/MAP.md` |
  | DECISIONS.md (Stage) | `arcs/arc-{n}/stages/stage-{n}/DECISIONS.md` |
  | SLICE.md | `arcs/arc-{n}/stages/stage-{n}/slices/slice-{n}/SLICE.md` |
  | DESIGN.md | `arcs/arc-{n}/stages/stage-{n}/slices/slice-{n}/DESIGN.md` |
  | RESEARCH.md | `arcs/arc-{n}/stages/stage-{n}/slices/slice-{n}/RESEARCH.md` |
  | stepNPLAN.md | `arcs/arc-{n}/stages/stage-{n}/slices/slice-{n}/step{step_number}PLAN.md` |
  | VERIFICATION.md | `arcs/arc-{n}/stages/stage-{n}/slices/slice-{n}/VERIFICATION.md` |
  | SUMMARY.md | `arcs/arc-{n}/stages/stage-{n}/slices/slice-{n}/SUMMARY.md` |
  | index.json | `index.json` |
  | STATE.md projections | `state/arcs.json`, `state/stages.json`, `state/slices.json`, `state/steps.json` |

  **SECTION 2: Templates (ART-02) — Embed templates for ALL 10 agent-authored artifact types:**

  For each template, include:
  - Template path: `.state/build/templates/{NAME}.tmpl`
  - Complete YAML frontmatter block with ALL fields from the corresponding Phase 400 pydantic model
  - Agent-owned fields with blank/empty defaults (e.g., `title: ""`, `goal: ""`, `depends_on: []`)
  - Projector-owned fields with their default values (e.g., `status: planned`, `step_count: 0`) — marked with comment `# ── Projector-owned (populated by daemon, NEVER edit) ──`
  - Body section with inline guidance comments for the agent (what to write in prose sections)
  - A Template↔Schema Cross-Reference table (from PATTERNS.md lines 193-206) confirming every template field exists in the corresponding Phase 400 pydantic model

  **Specific field requirements for each template** (extract exact field names from FRONTMATTER-SCHEMAS.md):

  ARC.md.tmpl: id, title, goal, success_criteria: [], depends_on: [], status: planned, stage_count: 0, shipped_stage_count: 0, completed_at: null
  STAGE.md.tmpl: id, title, goal, success_criteria: [], arc_id, depends_on: [], status: planned, slice_count: 0, shipped_slice_count: 0, completed_at: null
  SLICE.md.tmpl: id, title, goal, success_criteria: [], stage_id, depends_on: [], status: planned, step_count: 0, completed_step_count: 0, worktree_dir: null, worktree_branch: null, completed_at: null, deferred_reason: null, blocked_reason: null
  STEP.md.tmpl: step_number, title, goal, slice_id, plan_summary: "", verification_criteria: [], status: idle, completed_at: null, blocked_reason: null
  CRIT.md.tmpl (artifact introduced in Phase 401, not in Phase 400 frontmatter schemas — DESIGN these fields under Claude's discretion): parent_id (tier-level aggregate ID), criteria (array of {id: "CRIT-01", title, description, acceptance_test}), created_at, updated_at. Include comment explaining that CRIT.md holds numbered falsifiable requirements per D-401-03.
  MAP.md.tmpl (hybrid artifact — DESIGN these fields): parent_id (arc or stage ID), children (array of {id, goal, status_checkbox: "[ ]"}), generated_by, generated_at, last_updated. Include comment explaining that children array checkboxes are projector-updated per D-401-11.
  DECISIONS.md.tmpl: parent_id, decisions (array of {id: "D-{N}", title, context, decision, date}). Include comment about append-only policy per D-401-02.
  DESIGN.md.tmpl: slice_id, design_version, architecture_approach (prose), components (array of {name, responsibility, interfaces})
  RESEARCH.md.tmpl: slice_id, research_topics (array of {topic, decision, alternatives_considered, rationale}), standard_stack, confidence
  PLAN.md.tmpl (i.e., stepNPLAN.md — Step already has Phase 400 schema; this template is for the agent-facing plan body): reference StepFrontmatter from FRONTMATTER-SCHEMAS.md. Body section: "## Step Plan" heading with guidance to list implementation steps.

  **The Template↔Schema Cross-Reference table must explicitly verify** (per pitfall #2 in RESEARCH.md):
  - Every template frontmatter field maps to a field in the corresponding Phase 400 pydantic model
  - No template field exists that is absent from the pydantic model (would violate `extra="forbid"`)
  - Every required field from the pydantic model is present in the template
  - Note: CRIT.md, MAP.md, DECISIONS.md, DESIGN.md, and RESEARCH.md are Phase 401 artifacts without Phase 400 pydantic models — mark these as "Phase 401 design — frontmatter schema defined in this document"

  **SECTION 3: Immutability Rules (ART-03) — Create an immutability rules table:**

  Use the format from PATTERNS.md lines 626-639. For each artifact that has immutability constraints:

  | Artifact | Lock Trigger | Locked Fields | Mutable Fields | Enforcement |
  |----------|-------------|--------------|----------------|-------------|
  | ARC.md | Arc enters `in_progress` | depends_on, success_criteria | title, goal, description body | Snapshot-before-mutation (D-401-13) |
  | STAGE.md | Stage enters `in_progress` | depends_on, success_criteria | title, goal, description body | Snapshot-before-mutation |
  | SLICE.md | Slice enters `in_progress` | depends_on, success_criteria, goal | title, description body | Snapshot-before-mutation |
  | CRIT.md (Arc) | Arc enters `in_progress` | ALL fields (per D-401-12) | None after lock | Rejected, not warned |
  | CRIT.md (Stage) | Stage enters `in_progress` | ALL fields (per D-401-12) | None after lock | Rejected, not warned |
  | stepNPLAN.md | Slice enters `in_progress` (per D-401-12) | ALL fields | None after lock | Rejected, not warned |
  | DESIGN.md | Never | — | All fields always mutable | N/A |
  | RESEARCH.md | Never | — | All fields always mutable | N/A |
  | MAP.md | Hybrid — agent content mutable, projector checkboxes always writable by projector | — | Agent-written child goals mutable until parent enters in_progress; projector checkboxes always mutable by projector | Projector bypasses immutability lock per RESEARCH.md open question #5 |

  Verify that the CRIT.md lock rules align with D-401-12: "stepNPLAN.md and CRIT.md lock when Slice enters in_progress." Note: CRIT.md at Arc level locks when Arc enters in_progress; CRIT.md at Stage level locks when Stage enters in_progress (analogous application of the rule).

  **SECTION 4: Cross-References to Phase 400:**
  - TIER-ARC.md: Arc owned artifacts, state machine status values
  - TIER-STAGE.md: Stage owned artifacts, state machine status values
  - TIER-SLICE.md: Slice owned artifacts, state machine status values
  - TIER-STEP.md: Step owned artifacts, state machine status values
  - FRONTMATTER-SCHEMAS.md: All four pydantic frontmatter models (field definitions, status enums)
  - DESC-SEMANTICS.md: Abandon cascade, deferment, blocked state semantics (immutability interacts with these)
  - EVENT-TAXONOMY.md: Event types used as update triggers in catalog entries

  **SECTION 5: Threat Model Alignment** — map specification-level threats to mitigations:
  - T-401-01: Path inconsistency with directory tree → Mitigation: single source of truth for paths in this document; verify DIRECTORY-TREE.md paths match
  - T-401-02: Template frontmatter drift from Phase 400 schemas → Mitigation: Template↔Schema Cross-Reference table validates every field
  - T-401-03: Agent writing to projector-owned fields → Mitigation: clear projection-vs-authored ownership in every entry; `extra="forbid"` rejects unknown fields

  **CONSUMER NOTE:** Every catalog entry must note who reads this artifact and at what stage:
  - ARTIFACT-CATALOG.md entries: consumed by Plan 401-02 (DIRECTORY-TREE.md places artifacts on disk), Plan 401-03 (CROSS-REFERENCES.md cross-references artifacts), v41+ daemon projector
  - TEMPLATES.md: consumed by v41+ `/state-new` commands (arc, stage, slice) which render templates into editable artifact files
  - IMMUTABILITY-RULES.md: consumed by v41+ daemon snapshot-before-mutation protocol, v42 quality pipeline verifier

  **ANTI-PATTERNS TO AVOID:**
  - Do NOT reproduce Phase 400 state machines, event lists, or field tables verbatim. Reference them by doc+section.
  - Do NOT use different variable names for the same tier across entries (enforce arc-{n}/stage-{n}/slice-{n}/step-{n} everywhere).
  - Do NOT omit the Template↔Schema cross-reference table — this is the primary defense against frontmatter drift (pitfall #2).
  </action>

  <verify>
  <automated>
# Verify artifact catalog exists and has minimum content
test -f .planning/milestones/v40/phases/401/specs/ARTIFACT-CATALOG.md && echo "FILE_EXISTS"
# Verify all 13 artifact types are documented
grep -c "^### \(ARC\.md\|STAGE\.md\|SLICE\.md\|CRIT\.md\|MAP\.md\|DECISIONS\.md\|DESIGN\.md\|RESEARCH\.md\|stepNPLAN\.md\|VERIFICATION\.md\|SUMMARY\.md\|index\.json\|STATE\.md\)" .planning/milestones/v40/phases/401/specs/ARTIFACT-CATALOG.md
# Verify path variable notation is consistent (no {arc-id}, {stage-id}, etc.)
grep -c "{arc-id}\|{stage-id}\|{slice-id}\|{step-id}\|arc_id\|stage_id" .planning/milestones/v40/phases/401/specs/ARTIFACT-CATALOG.md || echo "PATH_NOTATION_CONSISTENT"
# Verify at least 8 template blocks exist
grep -c "\.tmpl" .planning/milestones/v40/phases/401/specs/ARTIFACT-CATALOG.md
# Verify immutability rules table exists
grep -c "Immutability" .planning/milestones/v40/phases/401/specs/ARTIFACT-CATALOG.md
  </automated>
  </verify>

  <acceptance_criteria>
  1. ARTIFACT-CATALOG.md exists at the specified path with ≥400 lines of content
  2. All 13 artifact types (ARC.md, STAGE.md, SLICE.md, CRIT.md, MAP.md, DECISIONS.md, DESIGN.md, RESEARCH.md, stepNPLAN.md, VERIFICATION.md, SUMMARY.md, index.json, STATE.md) have structured property-table entries
  3. Every entry includes all 13 property fields: Purpose, Owning Tier, Schema Owner, Created By, Creation Trigger, Updated By, Update Triggers, File Format, Frontmatter Model, Immutability, Cross-Refs From, Cross-Refs To, File Path
  4. Path variable notation is consistent across all entries: arc-{n}, stage-{n}, slice-{n}, step-{n} — grep finds zero instances of {arc-id}, {stage-id}, {slice-id}, {step-id}, arc_id, stage_id
  5. Templates exist for all 10 agent-authored artifact types with complete YAML frontmatter blocks
  6. Template↔Schema Cross-Reference table confirms every template field maps to a Phase 400 pydantic model field
  7. Immutability rules table covers all artifacts with lock triggers, locked fields, mutable fields, and enforcement mechanism
  8. D-401-12 lock rules (stepNPLAN.md and CRIT.md lock when Slice enters in_progress) are explicitly documented
  9. Phase 400 cross-reference section lists all 7 prerequisite specs with what this document uses from each
  10. `grep -c "arc-{n}" specs/ARTIFACT-CATALOG.md` returns ≥10 (consistent path variable notation)
  </acceptance_criteria>

  <done>
  ARTIFACT-CATALOG.md committed with all 13 artifact entries, 10+ templates with frontmatter cross-reference validation, and complete immutability rules table. Document self-consistent and references Phase 400 specs by ID/section (never reproduces them). Path variable notation uniform throughout.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create SCHEMA-OWNERSHIP.md — projection classification and schema validation rules</name>
  <files>.planning/milestones/v40/phases/401/specs/SCHEMA-OWNERSHIP.md</files>

  <read_first>
  - .planning/milestones/v40/phases/400/specs/FRONTMATTER-SCHEMAS.md (lines 198-303) — Field Ownership Rules table, cross-tier ID encoding, threat model alignment
  - .planning/milestones/v40/phases/401/401-CONTEXT.md (lines 30-37, 60-67) — D-401-09, D-401-10, D-401-11, D-401-14, D-401-15, D-401-16, D-401-17
  - .planning/milestones/v40/phases/401/401-PATTERNS.md (lines 602-619) — Field Ownership Classification Table pattern
  - .planning/milestones/v40/phases/401/specs/ARTIFACT-CATALOG.md (Task 1 output — for artifact type list and ownership classifications)
  </read_first>

  <action>
  Create `.planning/milestones/v40/phases/401/specs/SCHEMA-OWNERSHIP.md` — covering ART-04 (Projection vs Authored classification) and ART-05 (Schema Validation Rules).

  **STRUCTURE:**
  - Header: `> **Design contract for v41+ runtime schema validation and projection pipeline.**` + `Consumed by: v41+ (projector rebuild logic, daemon artifact validation), v42 (quality pipeline verifier), Phase 401 Plan 02 (INDEX-SCHEMA.md projector handler).`
  - Design Conventions section
  - Section 1: Projection vs Authored Classification (ART-04)
  - Section 2: MAP.md Hybrid Ownership Detail
  - Section 3: Schema Validation Rules (ART-05)
  - Cross-References to Phase 400 section

  **SECTION 1: Projection vs Authored Classification (ART-04):**

  Create a classification table mapping every artifact to its owner, with these columns:
  | Artifact | Owner | Classification | Rebuild Trigger | Fields Surviving Rebuild |
  |----------|-------|----------------|-----------------|--------------------------|

  Owner values: Agent (per D-401-09 list), Projector (per D-401-10 list), Hybrid (MAP.md per D-401-11).

  Explicit classification per D-401-09 and D-401-10:
  - AGENT-AUTHORED (10 types): ARC.md, STAGE.md, SLICE.md, CRIT.md, MAP.md (content), DESIGN.md, RESEARCH.md, stepNPLAN.md, VERIFICATION.md, SUMMARY.md, DECISIONS.md
  - PROJECTOR-REBUILT (2 types): index.json, consolidated STATE.md JSON projections
  - HYBRID (1 type): MAP.md — agent writes child entries and goals; projector updates checkboxes on state.stage.*/state.slice.* events (D-401-11)

  For each classification row, specify:
  - **Rebuild Trigger**: What events cause the projector to rebuild this artifact. For agent-authored artifacts: "Never rebuilt — agent writes directly; projector only updates status field." For projector artifacts: "Rebuilt on every state.* event (incremental) and on daemon startup (full rebuild)." For MAP.md: "Agent creates structure on /state-new arc stage; projector updates checkboxes on state.stage.*/state.slice.* events."
  - **Fields Surviving Rebuild**: What agent-written fields persist through a projector rebuild. For projector artifacts: "All fields are projector-computed — no agent fields survive." For agent artifacts: "All agent-owned fields survive; projector only overwrites its owned fields (status, *_count, completed_at)." For MAP.md: "Agent-written child goals survive; projector updates only checkbox status."

  **SECTION 2: MAP.md Hybrid Ownership Detail (D-401-11):**

  Document the MAP.md ownership model with precision:
  - MAP.md is created by the planner during the `/state-new arc` or `/state-new stage` flow (D-401-04)
  - The planner writes child entries: `{id: "stage-01", goal: "Implement OAuth handlers", status_checkbox: "[ ]"}`
  - Projector updates `status_checkbox: "[ ]"` → `"[x]"` when the child tier reaches its terminal state
  - Specific triggers:
    - Arc MAP.md checkboxes update on `state.stage.shipped` events (Stage reaches shipped)
    - Stage MAP.md checkboxes update on `state.slice.shipped` events (Slice reaches shipped)
  - The projector does NOT add, remove, or reorder child entries — only updates checkbox status
  - If a child is abandoned (D-13), the checkbox is updated to `"[!]"` (abandoned marker)

  **SECTION 3: Schema Validation Rules (ART-05):**

  Specify WHEN validation fires and WHAT it validates:

  **Validation Timing:**
  | Trigger Point | What Validates | Scope |
  |---------------|----------------|-------|
  | Artifact creation (CLI command) | Frontmatter schema against pydantic model | Single artifact |
  | Artifact read (daemon loads artifact) | Frontmatter schema + cross-reference ID resolution | Single artifact + referenced IDs |
  | Slice verify phase (/state-verify slice) | Full artifact consistency check | All Slice artifacts (SLICE.md through SUMMARY.md) |
  | Daemon startup (D-401-17) | Full consistency check via validate_consistency() | All artifacts |

  **Validation Checks (what pydantic `extra="forbid"` enforces):**
  1. Every frontmatter field must exist in the corresponding pydantic model — unknown fields rejected
  2. All required fields must be present (id, title, goal, and tier-specific required fields)
  3. Field types must match (str, list[str], list[dict[str, str]], int, None-able)
  4. Status values must be valid Literal members for that tier's state machine
  5. ID fields must match D-01 regex: `^arc-\d+$`, `^stage-\d+(\.\d+)?$`, `^slice-\d+(\.\d+)?$`, step_number is integer

  **Cross-Reference Validation (in addition to pydantic):**
  6. Every `depends_on[{id}]` reference must resolve to an existing ID in index.json (O(1) lookup per D-401-14)
  7. Edge types must be one of: blocks, soft, data (matching scheduler EdgeKind per Phase 400 D-12)
  8. Same-parent constraint (D-10): depends_on target must be a sibling within the same parent container
  9. No downward references (parent→child) — validated at schema level
  10. No cross-mode references (build→teach) — validated via BUILD_ONLY_EVENT_PREFIXES
  11. Descoped-ID references flagged (W005) — referrer surfaced with W-code
  12. Content-hash pinning: when artifact A creates cross-reference to artifact B, A records B's content hash. On subsequent reads, hash mismatch → WARNING if target is mutable, INFO if immutable.

  **Consumer attribution for each validation rule:**
  - Rules 1-5: consumed by v41+ pydantic frontmatter validator (reads Phase 400 models)
  - Rules 6-7: consumed by v41+ daemon artifact loader (reads index.json per D-401-14)
  - Rules 8-10: consumed by v41+ scheduler dependency resolver (reads Phase 400 D-10/D-12)
  - Rules 11-12: consumed by v41+ consistency validator (validate_consistency() per Plan 401-03)

  **SECTION 4: Cross-References to Phase 400:**
  - FRONTMATTER-SCHEMAS.md §Field Ownership Rules (lines 198-235): source of truth for which fields are agent vs projector
  - FRONTMATTER-SCHEMAS.md §Cross-Tier ID Encoding (lines 239-269): ID format validation rules
  - DESC-SEMANTICS.md §Abandon Cascade (D-13): descoped-ID detection triggers
  - FSM-TABLES.md: status Literal valid values per tier

  **ANTI-PATTERNS:**
  - Do NOT reproduce the full field ownership table from Phase 400 (lines 204-228). Reference it by section; add only the artifact-level classification not present in Phase 400.
  - Do NOT specify implementation details (e.g., Python validator decorator code, SQL queries for index lookup). Describe WHAT validation checks perform, not HOW they're implemented.
  </action>

  <verify>
  <automated>
# Verify file exists with minimum content
test -f .planning/milestones/v40/phases/401/specs/SCHEMA-OWNERSHIP.md && echo "FILE_EXISTS"
# Verify classification table covers all artifact types
grep -c "Agent\|Projector\|Hybrid" .planning/milestones/v40/phases/401/specs/SCHEMA-OWNERSHIP.md
# Verify MAP.md hybrid ownership section exists
grep -c "MAP\.md" .planning/milestones/v40/phases/401/specs/SCHEMA-OWNERSHIP.md
# Verify validation timing table exists
grep -c "Validation Timing\|validation timing\|When.*validat" .planning/milestones/v40/phases/401/specs/SCHEMA-OWNERSHIP.md
# Verify Phase 400 cross-references present
grep -c "FRONTMATTER-SCHEMAS\|DESC-SEMANTICS\|FSM-TABLES" .planning/milestones/v40/phases/401/specs/SCHEMA-OWNERSHIP.md
  </automated>
  </verify>

  <acceptance_criteria>
  1. SCHEMA-OWNERSHIP.md exists at the specified path with ≥150 lines of content
  2. Classification table covers all 13 artifact types from ARTIFACT-CATALOG.md with Agent/Projector/Hybrid classification
  3. MAP.md hybrid ownership section specifies exact checkbox trigger events (state.stage.shipped, state.slice.shipped) and the abandoned marker convention
  4. Schema validation rules specify 4 validation trigger points (creation, read, verify, startup) and 12 distinct validation checks
  5. Every validation check cites its consumer (which v41+ component implements it)
  6. Phase 400 FRONTMATTER-SCHEMAS.md field ownership table is referenced (not reproduced) — grep confirms `FRONTMATTER-SCHEMAS.md` appears in the document
  7. No Python implementation code or SQL queries in the document (design contract only)
  8. All 12 validation checks are categorized: 5 pydantic-level, 7 cross-reference/additional
  </acceptance_criteria>

  <done>
  SCHEMA-OWNERSHIP.md committed with complete projection-vs-authored classification, MAP.md hybrid ownership specification (with exact event trigger mapping), and 12 schema validation rules covering creation, read, verify, and startup timing. All Phase 400 references are by doc+section — no reproduction.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent → Artifact filesystem | Agent writes to `.state/build/` artifacts. Schema validation enforces that agent cannot write to projector-owned fields |
| Phase 400 specs → Phase 401 specs | Phase 401 specs must remain consistent with Phase 400 definitions. Drift creates contradictory design contracts |
| Template ↔ Pydantic Schema | Templates define what fields agents can write. Pydantic models define what fields are valid. Mismatch causes validation failures |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-401-01 | Tampering | ARTIFACT-CATALOG.md path definitions | mitigate | Single source of truth for paths in catalog. Every entry uses identical variable notation (arc-{n}). Cross-verify with DIRECTORY-TREE.md in Plan 02. |
| T-401-02 | Tampering | Template frontmatter fields | mitigate | Template↔Schema Cross-Reference table validates every template field against Phase 400 pydantic model. `extra="forbid"` rejects unknown fields at parse time. |
| T-401-03 | Elevation of Privilege | Agent writing projector-owned fields | mitigate | Field ownership classification (ART-04) explicitly separates agent and projector fields. Projector rebuilds overwrite any agent-written projector fields. Schema validation rejects agent writes to projector-owned fields. |
| T-401-04 | Tampering | Immutability bypass | mitigate | D-401-13 snapshot-before-mutation protocol at daemon level. Attempted mutation of locked artifact rejected (not warned). Spec documents which artifacts lock and when. |
| T-401-05 | Information Disclosure | Specification quality degradation | accept | Minor risk — Phase 401 is design documents, not runtime. Cross-reference consistency checks between Plan 01/02/03 catch contradictions. No sensitive data in design specs. |
</threat_model>

<verification>
## Phase-Level Verification (Plan 01)

**Cross-document consistency checks (manual review):**
1. Every artifact path in ARTIFACT-CATALOG.md uses `arc-{n}/stage-{n}/slice-{n}/step-{n}` notation — verify with grep for competing notations
2. Template↔Schema Cross-Reference table confirms every template frontmatter field exists in a Phase 400 pydantic model
3. Immutability rules table aligns with D-401-12 (stepNPLAN.md + CRIT.md lock on Slice in_progress) and D-401-13 (blocked, not warned)
4. SCHEMA-OWNERSHIP.md classification matches D-401-09 (agent-authored = 10 types), D-401-10 (projector = 2 types), D-401-11 (MAP.md hybrid)
5. Schema validation rules reference the correct pydantic models from Phase 400 FRONTMATTER-SCHEMAS.md
</verification>

<success_criteria>
**ROADMAP SC1 mapped:** ARTIFACT-CATALOG.md documents every file type across all four tiers with purpose, owning tier, schema ownership, creation/update triggers, and file format. All 13 artifact types covered with structured property tables.
**ROADMAP SC2 mapped:** Templates exist for all 10 agent-authored artifact file types with validated frontmatter structure. Template↔Schema cross-reference table confirms no field drift.
**ROADMAP SC4 mapped:** Immutability rules specified per tier (ART-03). Schema validation rules documented with timing and checks (ART-05). All 17 locked decisions reflected in specification content.
</success_criteria>

<output>
After completion, create `.planning/milestones/v40/phases/401/401-01-SUMMARY.md` with:
- Created files list
- Decisions made under Claude's discretion (CRIT.md/MAP.md/DECISIONS.md frontmatter field design, template body guidance text)
- Phase 400 cross-reference index
- Consumer audit: which downstream plans consume each spec section
- Any deviations from CONTEXT.md decisions (none expected — this plan implements locked decisions)
</output>
